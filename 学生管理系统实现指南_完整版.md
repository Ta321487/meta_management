# 学生管理系统实现指南（完整版）

本指南详细说明如何使用元数据管理系统快速构建一个功能完整的学生管理系统，包含学生、班级、课程、成绩等核心管理功能。通过元数据配置和代码生成，您可以在不编写大量重复代码的情况下，实现完整的业务功能。

## 系统概述

### 元数据管理系统介绍

当前项目是一个功能强大的元数据管理系统，它允许用户通过图形化界面定义完整的业务模型，包括模块类型、模块结构、功能节点、表结构、字段配置、表关联关系等，然后自动生成完整的业务代码，包括：
- 数据库建表SQL
- Java后端代码（Entity、Controller、Service、Mapper等）
- Vue前端页面（列表页、表单页、详情页等）
- 路由配置和权限设置

系统采用模块化设计，支持复杂业务场景的快速构建和定制。核心技术栈包括：
- 后端：Spring Boot 2.7.18, MyBatis, MySQL 8.0, Freemarker
- 前端：Vue 3, Element Plus, Vue Router, Axios

### 学生管理系统目标

通过配置元数据，我们将实现一个功能完整的学生管理系统，包括：
- 学生信息的基础管理（增删改查）
- 班级信息的管理
- 课程信息的管理
- 成绩录入和统计分析
- 完整的表关联关系配置和使用
- 完整的前后端代码生成和部署

本指南将详细介绍如何利用元数据管理系统的各种功能，快速构建和定制学生管理系统。

## 环境准备

### 1. 运行元数据管理系统

1. 确保已安装以下环境：
   - JDK 17（必须，系统依赖Java 17）
   - Maven 3.6+ 
   - Node.js 14+ 和 npm 6+
   - MySQL 8.0 数据库

2. 运行数据库初始化脚本创建元数据数据库：
   ```bash
   # 在MySQL命令行中执行
   source d:/Java/meta-management/database/init.sql
   ```
   该脚本会创建`metadata_db`数据库及相关表结构，并初始化默认管理员账号。

3. 配置数据库连接（可选，如需修改默认配置）：
   编辑`src/main/resources/application.properties`文件，修改数据库连接信息。

4. 启动后端服务：
   ```bash
   # 在项目根目录执行
   mvn spring-boot:run
   ```
   或打包后运行：
   ```bash
   mvn package
   java -jar target/metadata-system-1.0.0.war
   ```

5. 启动前端服务：
   ```bash
   # 进入frontend目录
   cd frontend
   npm install
   npm run serve
   ```

6. 访问系统：
   - 后端API地址：`http://localhost:8080/metadata-system/api/`
   - 前端访问地址：`http://localhost:8080/metadata-system/`
   - 登录账号：admin
   - 登录密码：123456

## 实现步骤

### 前置准备：模块类型管理

在创建模块之前，需要了解模块类型的管理。模块类型类似于后台管理系统的左侧导航区域，用于组织和分类不同功能模块，决定了模块的基础功能和行为。系统初始化时已创建了几种常用的模块类型，您也可以根据需要自定义新的模块类型。

#### 1. 查看现有模块类型
1. 登录系统后，在左侧菜单中点击顶部的 **模块类型管理**（第一个菜单项）
2. 系统将显示所有已配置的模块类型列表，包括系统初始化时创建的模块类型：
   - DATA_MANAGE：数据管理型，支持标准CRUD操作
   - PROCESS_APPROVE：流程审批型，支持流程审批功能
   - STAT_REPORT：统计报表型，支持统计报表功能
   - BATCH_OPERATE：批量操作型，支持批量导入导出功能

#### 2. 创建新模块类型
根据架构设计，我们需要创建一个新的模块类型用于成绩管理：
1. 在模块类型管理页面点击 **新增类型** 按钮，会弹出一个「新增类型」对话框
2. 在对话框中填写以下信息：
   - **类型编码**：`SCORE_MANAGE`
   - **类型名称**：`成绩管理型`
   - **默认功能节点**：`LIST_PAGE,FORM_PAGE,DETAIL_PAGE,STATS_PAGE`
   - **描述**：`用于成绩录入、统计和管理的模块类型`
3. 点击 **保存** 按钮

### 第一步：创建基础信息管理模块

#### 1. 创建学生管理模块
1. 点击左侧菜单的 **模块管理**
2. 点击页面顶部的 **新增模块** 按钮
3. 填写模块信息：
   - **模块编码**：`STUDENT_MODULE`（必须唯一）
   - **模块名称**：`学生管理`
   - **模块类型**：选择 `DATA_MANAGE`（数据管理型）
   - **描述**：`学生信息管理模块，支持学生信息的增删改查操作`
   - **排序**：1
   - **图标**：`el-icon-user`
   - **路由路径**：`/student`
   - **组件路径**：`views/student/index`
4. 点击 **保存** 按钮

#### 2. 创建班级管理模块
1. 点击页面顶部的 **新增模块** 按钮
2. 填写模块信息：
   - **模块编码**：`CLASS_MODULE`
   - **模块名称**：`班级管理`
   - **模块类型**：选择 `DATA_MANAGE`
   - **描述**：`班级信息管理模块`
   - **排序**：2
   - **图标**：`el-icon-office-building`
   - **路由路径**：`/class`
   - **组件路径**：`views/class/index`
3. 点击 **保存** 按钮

#### 3. 创建教师管理模块
1. 点击页面顶部的 **新增模块** 按钮
2. 填写模块信息：
   - **模块编码**：`TEACHER_MODULE`
   - **模块名称**：`教师管理`
   - **模块类型**：选择 `DATA_MANAGE`
   - **描述**：`教师信息管理模块`
   - **排序**：3
   - **图标**：`el-icon-s-custom`
   - **路由路径**：`/teacher`
   - **组件路径**：`views/teacher/index`
3. 点击 **保存** 按钮

### 第二步：创建课程管理模块

1. 点击页面顶部的 **新增模块** 按钮
2. 填写模块信息：
   - **模块编码**：`COURSE_MODULE`
   - **模块名称**：`课程管理`
   - **模块类型**：选择 `DATA_MANAGE`
   - **描述**：`课程信息管理模块，支持课程信息的增删改查和成绩管理`
   - **排序**：4
   - **图标**：`el-icon-edit-outline`
   - **路由路径**：`/course`
   - **组件路径**：`views/course/index`
3. 点击 **保存** 按钮

### 第三步：创建成绩管理模块

1. 点击页面顶部的 **新增模块** 按钮
2. 填写模块信息：
   - **模块编码**：`SCORE_MODULE`
   - **模块名称**：`成绩管理`
   - **模块类型**：选择 `SCORE_MANAGE`
   - **描述**：`成绩管理模块，支持成绩录入、统计和分析`
   - **排序**：5
   - **图标**：`el-icon-data-line`
   - **路由路径**：`/score`
   - **组件路径**：`views/score/index`
3. 点击 **保存** 按钮

### 第四步：创建表结构

#### 1. 创建班级表
1. 点击左侧菜单的 **表管理**
2. 点击 **新增表** 按钮
3. 填写表信息：
   - **表编码**：`CLASS_TABLE`
   - **表名称**：`班级信息表`
   - **主键策略**：`AUTO`（自增）
   - **描述**：`存储班级基本信息`
4. 点击 **保存** 按钮

#### 2. 创建教师表
1. 点击 **新增表** 按钮
2. 填写表信息：
   - **表编码**：`TEACHER_TABLE`
   - **表名称**：`教师信息表`
   - **主键策略**：`AUTO`（自增）
   - **描述**：`存储教师基本信息`
3. 点击 **保存** 按钮

#### 3. 创建学生表
1. 点击 **新增表** 按钮
2. 填写表信息：
   - **表编码**：`STUDENT_TABLE`
   - **表名称**：`学生信息表`
   - **主键策略**：`AUTO`（自增）
   - **描述**：`存储学生基本信息`
3. 点击 **保存** 按钮

#### 4. 创建课程表
1. 点击 **新增表** 按钮
2. 填写表信息：
   - **表编码**：`COURSE_TABLE`
   - **表名称**：`课程信息表`
   - **主键策略**：`AUTO`（自增）
   - **描述**：`存储课程基本信息`
3. 点击 **保存** 按钮

#### 5. 创建成绩表
1. 点击 **新增表** 按钮
2. 填写表信息：
   - **表编码**：`SCORE_TABLE`
   - **表名称**：`成绩信息表`
   - **主键策略**：`AUTO`（自增）
   - **描述**：`存储学生课程成绩信息`
3. 点击 **保存** 按钮

### 第五步：配置表字段

#### 1. 配置班级表字段
1. 点击左侧菜单的 **字段管理**
2. 在表选择下拉框中选择 `CLASS_TABLE`
3. 点击 **新增字段** 按钮，依次添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 | 校验规则 |
|---------|---------|---------|--------|---------|---------|------|----------|
| CLASS_CODE | class_code | varchar(50) | 班级编号 | 是 | input | 1 | `{"pattern": "^\\w{4,20}$", "message": "班级编号只能包含字母数字下划线，长度4-20位"}` |
| CLASS_NAME | class_name | varchar(100) | 班级名称 | 是 | input | 2 | |
| GRADE | grade | varchar(50) | 年级 | 否 | input | 3 | |
| HEAD_TEACHER_ID | head_teacher_id | bigint | 班主任ID | 否 | select | 4 | |
| STUDENT_COUNT | student_count | int | 学生人数 | 否 | number | 5 | `{"min": 0, "message": "学生人数不能为负数"}` |

#### 2. 配置教师表字段
1. 在表选择下拉框中选择 `TEACHER_TABLE`
2. 点击 **新增字段** 按钮，依次添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 | 校验规则 |
|---------|---------|---------|--------|---------|---------|------|----------|
| TEACHER_CODE | teacher_code | varchar(50) | 教师编号 | 是 | input | 1 | `{"pattern": "^\\w{4,20}$", "message": "教师编号只能包含字母数字下划线，长度4-20位"}` |
| TEACHER_NAME | teacher_name | varchar(100) | 教师姓名 | 是 | input | 2 | |
| TEACHER_GENDER | gender | varchar(10) | 性别 | 否 | select | 3 | `{"options": ["男", "女"]}` |
| TEACHER_TITLE | title | varchar(50) | 职称 | 否 | input | 4 | |
| TEACHER_PHONE | phone | varchar(20) | 手机号 | 否 | input | 5 | `{"pattern": "^1[3-9]\\d{9}$", "message": "请输入正确的手机号"}` |
| TEACHER_EMAIL | email | varchar(100) | 邮箱 | 否 | input | 6 | `{"pattern": "^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$", "message": "请输入正确的邮箱地址"}` |

#### 3. 配置学生表字段
1. 在表选择下拉框中选择 `STUDENT_TABLE`
2. 点击 **新增字段** 按钮，依次添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 | 校验规则 |
|---------|---------|---------|--------|---------|---------|------|----------|
| STU_CODE | student_code | varchar(50) | 学号 | 是 | input | 1 | `{"pattern": "^\\d{10}$", "message": "学号必须为10位数字"}` |
| STU_NAME | student_name | varchar(100) | 姓名 | 是 | input | 2 | |
| STU_GENDER | gender | varchar(10) | 性别 | 是 | select | 3 | `{"options": ["男", "女"]}` |
| STU_AGE | age | int | 年龄 | 否 | number | 4 | `{"min": 1, "max": 100, "message": "年龄必须在1-100之间"}` |
| STU_PHONE | phone | varchar(20) | 手机号 | 否 | input | 5 | `{"pattern": "^1[3-9]\\d{9}$", "message": "请输入正确的手机号"}` |
| STU_EMAIL | email | varchar(100) | 邮箱 | 否 | input | 6 | `{"pattern": "^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$", "message": "请输入正确的邮箱地址"}` |
| CLASS_ID | class_id | bigint | 班级ID | 否 | select | 7 | |
| STU_ENTRY_DATE | entry_date | date | 入学日期 | 否 | datepicker | 8 | |
| STU_ADDRESS | address | varchar(200) | 地址 | 否 | input | 9 | |
| STU_STATUS | status | varchar(20) | 状态 | 否 | select | 10 | `{"options": ["在读", "休学", "毕业", "退学"]}` |


#### 4. 配置课程表字段
1. 在表选择下拉框中选择 `COURSE_TABLE`
2. 点击 **新增字段** 按钮，依次添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 | 校验规则 |
|---------|---------|---------|--------|---------|---------|------|----------|
| COURSE_CODE | course_code | varchar(50) | 课程编号 | 是 | input | 1 | `{"pattern": "^\\w{4,20}$", "message": "课程编号只能包含字母数字下划线，长度4-20位"}` |
| COURSE_NAME | course_name | varchar(100) | 课程名称 | 是 | input | 2 | |
| COURSE_TYPE | course_type | varchar(50) | 课程类型 | 否 | select | 3 | `{"options": ["必修课", "选修课"]}` |
| CREDITS | credits | decimal(4,1) | 学分 | 否 | number | 4 | `{"min": 0, "max": 10, "message": "学分必须在0-10之间"}` |
| HOURS | hours | int | 课时 | 否 | number | 5 | `{"min": 0, "message": "课时不能为负数"}` |
| COURSE_DESCRIPTION | description | text | 课程描述 | 否 | textarea | 6 | |
| TEACHER_ID | teacher_id | bigint | 授课教师ID | 否 | select | 7 | |

#### 5. 配置成绩表字段
1. 在表选择下拉框中选择 `SCORE_TABLE`
2. 点击 **新增字段** 按钮，依次添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 | 校验规则 |
|---------|---------|---------|--------|---------|---------|------|----------|
| STUDENT_ID | student_id | bigint | 学生ID | 是 | select | 1 | |
| COURSE_ID | course_id | bigint | 课程ID | 是 | select | 2 | |
| SCORE | score | decimal(5,2) | 分数 | 否 | number | 3 | `{"min": 0, "max": 100, "message": "分数必须在0-100之间"}` |
| EXAM_DATE | exam_date | date | 考试日期 | 否 | datepicker | 4 | |
| SEMESTER | semester | varchar(50) | 学期 | 否 | input | 5 | |
| REMARK | remark | text | 备注 | 否 | textarea | 6 | |

### 第六步：配置表关联关系

#### 1. 配置教师和班级的一对多关系
1. 点击左侧菜单的 **表关联关系管理**
2. 点击 **新增关联** 按钮
3. 配置教师-班级关联：
   - **关联名称**：`教师_班级`
   - **主表**：选择 `TEACHER_TABLE`
   - **从表**：选择 `CLASS_TABLE`
   - **关联类型**：选择 `ONE_TO_MANY`
   - **主表关联字段**：`TEACHER_ID`
   - **从表关联字段**：`HEAD_TEACHER_ID`
   - **显示字段**：`CLASS_NAME`
   - **描述**：`一个教师可以担任多个班级的班主任`
4. 点击 **保存** 按钮

#### 2. 配置班级和学生的一对多关系
1. 点击 **新增关联** 按钮
2. 配置班级-学生关联：
   - **关联名称**：`班级_学生`
   - **主表**：选择 `CLASS_TABLE`
   - **从表**：选择 `STUDENT_TABLE`
   - **关联类型**：选择 `ONE_TO_MANY`
   - **主表关联字段**：`CLASS_ID`
   - **从表关联字段**：`CLASS_ID`
   - **显示字段**：`STUDENT_NAME`
   - **描述**：`一个班级包含多个学生`
3. 点击 **保存** 按钮

#### 3. 配置学生和班级的多对一关系
1. 点击 **新增关联** 按钮
2. 配置学生-班级关联：
   - **关联名称**：`学生_班级`
   - **主表**：选择 `STUDENT_TABLE`
   - **从表**：选择 `CLASS_TABLE`
   - **关联类型**：选择 `MANY_TO_ONE`
   - **主表关联字段**：`CLASS_ID`
   - **从表关联字段**：`CLASS_ID`
   - **显示字段**：`CLASS_NAME`
   - **描述**：`多个学生属于一个班级`
3. 点击 **保存** 按钮

#### 4. 配置教师和课程的一对多关系
1. 点击 **新增关联** 按钮
2. 配置教师-课程关联：
   - **关联名称**：`教师_课程`
   - **主表**：选择 `TEACHER_TABLE`
   - **从表**：选择 `COURSE_TABLE`
   - **关联类型**：选择 `ONE_TO_MANY`
   - **主表关联字段**：`TEACHER_ID`
   - **从表关联字段**：`TEACHER_ID`
   - **显示字段**：`COURSE_NAME`
   - **描述**：`一个教师可以教授多门课程`
3. 点击 **保存** 按钮

#### 5. 配置课程和学生成绩的一对多关系（通过成绩表）
1. 点击 **新增关联** 按钮
2. 配置课程-成绩关联：
   - **关联名称**：`课程_成绩`
   - **主表**：选择 `COURSE_TABLE`
   - **从表**：选择 `SCORE_TABLE`
   - **关联类型**：选择 `ONE_TO_MANY`
   - **主表关联字段**：`COURSE_ID`
   - **从表关联字段**：`COURSE_ID`
   - **描述**：`一门课程有多个学生成绩`
3. 点击 **保存** 按钮

#### 6. 配置学生和成绩的一对多关系
1. 点击 **新增关联** 按钮
2. 配置学生-成绩关联：
   - **关联名称**：`学生_成绩`
   - **主表**：选择 `STUDENT_TABLE`
   - **从表**：选择 `SCORE_TABLE`
   - **关联类型**：选择 `ONE_TO_MANY`
   - **主表关联字段**：`STU_ID`
   - **从表关联字段**：`STUDENT_ID`
   - **描述**：`一个学生有多门课程成绩`
3. 点击 **保存** 按钮

### 第七步：关联模块和表

#### 1. 关联学生管理模块和学生表
1. 在 **模块管理** 页面，点击 `STUDENT_MODULE` 模块的 **编辑** 按钮
2. 在弹出的表单中，找到 **关联表** 部分
3. 点击添加按钮，选择 `STUDENT_TABLE`
4. 点击 **保存** 按钮

#### 2. 关联班级管理模块和班级表
1. 在 **模块管理** 页面，点击 `CLASS_MODULE` 模块的 **编辑** 按钮
2. 在弹出的表单中，找到 **关联表** 部分
3. 点击添加按钮，选择 `CLASS_TABLE`
4. 点击 **保存** 按钮

#### 3. 关联教师管理模块和教师表
1. 在 **模块管理** 页面，点击 `TEACHER_MODULE` 模块的 **编辑** 按钮
2. 在弹出的表单中，找到 **关联表** 部分
3. 点击添加按钮，选择 `TEACHER_TABLE`
4. 点击 **保存** 按钮

#### 4. 关联课程管理模块和课程表
1. 在 **模块管理** 页面，点击 `COURSE_MODULE` 模块的 **编辑** 按钮
2. 在弹出的表单中，找到 **关联表** 部分
3. 点击添加按钮，选择 `COURSE_TABLE`
4. 点击 **保存** 按钮

#### 5. 关联成绩管理模块和成绩表
1. 在 **模块管理** 页面，点击 `SCORE_MODULE` 模块的 **编辑** 按钮
2. 在弹出的表单中，找到 **关联表** 部分
3. 点击添加按钮，选择 `SCORE_TABLE`
4. 点击 **保存** 按钮

### 第八步：配置功能节点

#### 1. 配置学生管理模块功能节点

##### 1.1 学生列表页节点
1. 点击左侧菜单的 **功能节点管理**
2. 点击 **新增节点** 按钮
3. 配置列表页节点：
   - **节点编码**：`STUDENT_LIST`
   - **节点名称**：`学生列表页`
   - **所属模块**：选择 `STUDENT_MODULE`
   - **节点类型**：`LIST_PAGE`
   - **关联表**：`STUDENT_TABLE`
   - **路由路径**：`/student/list`
   - **组件路径**：`views/student/List.vue`
   - **排序**：1
   - **图标**：`el-icon-document`
   - **是否在菜单显示**：是
4. 点击 **保存** 按钮

##### 1.2 学生表单页节点
1. 点击 **新增节点** 按钮
2. 配置表单页节点：
   - **节点编码**：`STUDENT_FORM`
   - **节点名称**：`学生表单页`
   - **所属模块**：选择 `STUDENT_MODULE`
   - **节点类型**：`FORM_PAGE`
   - **关联表**：`STUDENT_TABLE`
   - **路由路径**：`/student/form/:id?`
   - **组件路径**：`views/student/Form.vue`
   - **排序**：2
   - **是否在菜单显示**：否
3. 点击 **保存** 按钮

##### 1.3 学生详情页节点
1. 点击 **新增节点** 按钮
2. 配置详情页节点：
   - **节点编码**：`STUDENT_DETAIL`
   - **节点名称**：`学生详情页`
   - **所属模块**：选择 `STUDENT_MODULE`
   - **节点类型**：`DETAIL_PAGE`
   - **关联表**：`STUDENT_TABLE`
   - **路由路径**：`/student/detail/:id`
   - **组件路径**：`views/student/Detail.vue`
   - **排序**：3
   - **是否在菜单显示**：否
3. 点击 **保存** 按钮

##### 1.4 学生成绩管理节点
1. 点击 **新增节点** 按钮
2. 配置成绩管理节点：
   - **节点编码**：`STUDENT_SCORE`
   - **节点名称**：`学生成绩管理`
   - **所属模块**：选择 `STUDENT_MODULE`
   - **节点类型**：`CUSTOM_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/student/scores/:id`
   - **组件路径**：`views/student/Scores.vue`
   - **排序**：4
   - **是否在菜单显示**：否
3. 点击 **保存** 按钮

#### 2. 配置班级管理模块功能节点

##### 2.1 班级列表页节点
1. 点击 **新增节点** 按钮
2. 配置列表页节点：
   - **节点编码**：`CLASS_LIST`
   - **节点名称**：`班级列表页`
   - **所属模块**：选择 `CLASS_MODULE`
   - **节点类型**：`LIST_PAGE`
   - **关联表**：`CLASS_TABLE`
   - **路由路径**：`/class/list`
   - **组件路径**：`views/class/List.vue`
   - **排序**：1
   - **图标**：`el-icon-document`
   - **是否在菜单显示**：是
3. 点击 **保存** 按钮

##### 2.2 班级表单页节点
1. 点击 **新增节点** 按钮
2. 配置表单页节点：
   - **节点编码**：`CLASS_FORM`
   - **节点名称**：`班级表单页`
   - **所属模块**：选择 `CLASS_MODULE`
   - **节点类型**：`FORM_PAGE`
   - **关联表**：`CLASS_TABLE`
   - **路由路径**：`/class/form/:id?`
   - **组件路径**：`views/class/Form.vue`
   - **排序**：2
   - **是否在菜单显示**：否
3. 点击 **保存** 按钮

##### 2.3 班级学生管理节点
1. 点击 **新增节点** 按钮
2. 配置学生管理节点：
   - **节点编码**：`CLASS_STUDENTS`
   - **节点名称**：`班级学生管理`
   - **所属模块**：选择 `CLASS_MODULE`
   - **节点类型**：`CUSTOM_PAGE`
   - **关联表**：`STUDENT_TABLE`
   - **路由路径**：`/class/students/:id`
   - **组件路径**：`views/class/Students.vue`
   - **排序**：3
   - **是否在菜单显示**：否
3. 点击 **保存** 按钮

#### 3. 配置课程管理模块功能节点

##### 3.1 课程列表页节点
1. 点击 **新增节点** 按钮
2. 配置列表页节点：
   - **节点编码**：`COURSE_LIST`
   - **节点名称**：`课程列表页`
   - **所属模块**：选择 `COURSE_MODULE`
   - **节点类型**：`LIST_PAGE`
   - **关联表**：`COURSE_TABLE`
   - **路由路径**：`/course/list`
   - **组件路径**：`views/course/List.vue`
   - **排序**：1
   - **图标**：`el-icon-document`
   - **是否在菜单显示**：是
3. 点击 **保存** 按钮

##### 3.2 课程表单页节点
1. 点击 **新增节点** 按钮
2. 配置表单页节点：
   - **节点编码**：`COURSE_FORM`
   - **节点名称**：`课程表单页`
   - **所属模块**：选择 `COURSE_MODULE`
   - **节点类型**：`FORM_PAGE`
   - **关联表**：`COURSE_TABLE`
   - **路由路径**：`/course/form/:id?`
   - **组件路径**：`views/course/Form.vue`
   - **排序**：2
   - **是否在菜单显示**：否
3. 点击 **保存** 按钮

##### 3.3 课程成绩管理节点
1. 点击 **新增节点** 按钮
2. 配置成绩管理节点：
   - **节点编码**：`COURSE_SCORES`
   - **节点名称**：`课程成绩管理`
   - **所属模块**：选择 `COURSE_MODULE`
   - **节点类型**：`CUSTOM_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/course/scores/:id`
   - **组件路径**：`views/course/Scores.vue`
   - **排序**：3
   - **是否在菜单显示**：否
3. 点击 **保存** 按钮

#### 4. 配置成绩管理模块功能节点

##### 4.1 成绩列表页节点
1. 点击 **新增节点** 按钮
2. 配置列表页节点：
   - **节点编码**：`SCORE_LIST`
   - **节点名称**：`成绩列表页`
   - **所属模块**：选择 `SCORE_MODULE`
   - **节点类型**：`LIST_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/score/list`
   - **组件路径**：`views/score/List.vue`
   - **排序**：1
   - **图标**：`el-icon-document`
   - **是否在菜单显示**：是
3. 点击 **保存** 按钮

##### 4.2 成绩表单页节点
1. 点击 **新增节点** 按钮
2. 配置表单页节点：
   - **节点编码**：`SCORE_FORM`
   - **节点名称**：`成绩表单页`
   - **所属模块**：选择 `SCORE_MODULE`
   - **节点类型**：`FORM_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/score/form/:id?`
   - **组件路径**：`views/score/Form.vue`
   - **排序**：2
   - **是否在菜单显示**：否
3. 点击 **保存** 按钮

##### 4.3 成绩批量录入节点
1. 点击 **新增节点** 按钮
2. 配置批量录入节点：
   - **节点编码**：`SCORE_BATCH_INPUT`
   - **节点名称**：`成绩批量录入`
   - **所属模块**：选择 `SCORE_MODULE`
   - **节点类型**：`CUSTOM_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/score/batchInput`
   - **组件路径**：`views/score/BatchInput.vue`
   - **排序**：3
   - **图标**：`el-icon-upload`
   - **是否在菜单显示**：是
3. 点击 **保存** 按钮

##### 4.4 成绩统计分析节点
1. 点击 **新增节点** 按钮
2. 配置统计分析节点：
   - **节点编码**：`SCORE_STATISTICS`
   - **节点名称**：`成绩统计分析`
   - **所属模块**：选择 `SCORE_MODULE`
   - **节点类型**：`REPORT_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/score/statistics`
   - **组件路径**：`views/score/Statistics.vue`
   - **排序**：4
   - **图标**：`el-icon-data-line`
   - **是否在菜单显示**：是
3. 点击 **保存** 按钮

##### 4.5 成绩单导出节点
1. 点击 **新增节点** 按钮
2. 配置导出节点：
   - **节点编码**：`SCORE_EXPORT`
   - **节点名称**：`成绩单导出`
   - **所属模块**：选择 `SCORE_MODULE`
   - **节点类型**：`BATCH_EXPORT_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/score/export`
   - **组件路径**：`views/score/Export.vue`
   - **排序**：5
   - **图标**：`el-icon-download`
   - **是否在菜单显示**：是
3. 点击 **保存** 按钮

### 第九步：配置业务规则

#### 1. 配置学生相关业务规则
1. 点击左侧菜单的 **业务规则管理**
2. 点击 **新增规则** 按钮
3. 添加学号唯一性规则：
   - 规则编码：`RULE_STU_CODE_UNIQUE`
   - 模块编码：`STUDENT_MODULE`
   - 规则类型：`VALIDATION_RULE`
   - 规则内容：
     ```json
     {
       "field": "student_code",
       "type": "unique",
       "message": "学号已存在"
     }
     ```
   - 描述：`确保学生学号的唯一性`
4. 点击 **保存** 按钮

#### 2. 配置课程相关业务规则
1. 点击 **新增规则** 按钮
2. 添加课程编码唯一性规则：
   - 规则编码：`RULE_COURSE_CODE_UNIQUE`
   - 模块编码：`COURSE_MODULE`
   - 规则类型：`VALIDATION_RULE`
   - 规则内容：
     ```json
     {
       "field": "course_code",
       "type": "unique",
       "message": "课程编码已存在"
     }
     ```
   - 描述：`确保课程编码的唯一性`
3. 点击 **保存** 按钮

#### 3. 配置成绩相关业务规则
1. 点击 **新增规则** 按钮
2. 添加成绩唯一性规则（同一个学生同一门课程只能有一条成绩记录）：
   - 规则编码：`RULE_SCORE_UNIQUE`
   - 模块编码：`SCORE_MODULE`
   - 规则类型：`VALIDATION_RULE`
   - 规则内容：
     ```json
     {
       "fields": ["student_id", "course_id"],
       "type": "unique_combo",
       "message": "该学生的该课程成绩已存在"
     }
     ```
   - 描述：`确保每个学生每门课程只有一条成绩记录`
3. 点击 **保存** 按钮

### 第十步：生成业务系统代码

现在我们已经完成了所有必要的配置，可以生成实际的业务代码了。

#### 1. 生成学生管理相关代码
1. 点击左侧菜单的 **代码生成**
2. 在 **选择表** 下拉框中选择 `STUDENT_TABLE`
3. 在 **包名** 输入框中输入 `com.student.management`
4. 勾选 **包含关联关系** 选项
5. 点击 **生成所有代码** 按钮
6. 生成完成后，查看生成的代码并复制到剪贴板

#### 2. 生成班级管理相关代码
1. 在 **选择表** 下拉框中选择 `CLASS_TABLE`
2. 在 **包名** 输入框中输入 `com.student.management.classes`
3. 勾选 **包含关联关系** 选项
4. 点击 **生成所有代码** 按钮
5. 生成完成后，查看生成的代码并复制到剪贴板

#### 3. 生成教师管理相关代码
1. 在 **选择表** 下拉框中选择 `TEACHER_TABLE`
2. 在 **包名** 输入框中输入 `com.student.management.teacher`
3. 勾选 **包含关联关系** 选项
4. 点击 **生成所有代码** 按钮
5. 生成完成后，查看生成的代码并复制到剪贴板

#### 4. 生成课程管理相关代码
1. 在 **选择表** 下拉框中选择 `COURSE_TABLE`
2. 在 **包名** 输入框中输入 `com.student.management.course`
3. 勾选 **包含关联关系** 选项
4. 点击 **生成所有代码** 按钮
5. 生成完成后，查看生成的代码并复制到剪贴板

#### 5. 生成成绩管理相关代码
1. 在 **选择表** 下拉框中选择 `SCORE_TABLE`
2. 在 **包名** 输入框中输入 `com.student.management.score`
3. 勾选 **包含关联关系** 选项
4. 点击 **生成所有代码** 按钮
5. 生成完成后，查看生成的代码并复制到剪贴板

## 第十一步：部署学生管理系统

### 11.1 数据库配置

1. 创建学生管理系统数据库：
   ```sql
   CREATE DATABASE IF NOT EXISTS `student_management` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   USE `student_management`;
   ```

2. 执行生成的SQL建表语句，为每个表创建相应的表结构

3. 配置数据库连接：
   在目标项目的`application.properties`或`application.yml`中配置数据库连接信息：
   ```properties
   spring.datasource.url=jdbc:mysql://localhost:3306/student_management?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
   spring.datasource.username=root
   spring.datasource.password=your_password
   spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
   ```

4. 配置MyBatis：
   ```properties
   mybatis.mapper-locations=classpath:mapper/*.xml
   mybatis.type-aliases-package=com.student.management
   ```

### 11.2 后端部署

1. 创建一个新的Spring Boot项目（或使用现有项目），确保使用兼容的Spring Boot版本（建议2.7.x）

2. 创建以下包结构：
   ```
   com.student.management
   ├── common        # 通用类
   ├── entity        # 学生实体类
   ├── controller    # 学生控制器
   ├── service       # 学生服务
   ├── mapper        # 学生数据访问
   ├── classes       # 班级管理相关
   │   ├── entity
   │   ├── controller
   │   ├── service
   │   └── mapper
   ├── teacher       # 教师管理相关
   │   ├── entity
   │   ├── controller
   │   ├── service
   │   └── mapper
   ├── course        # 课程管理相关
   │   ├── entity
   │   ├── controller
   │   ├── service
   │   └── mapper
   └── score         # 成绩管理相关
       ├── entity
       ├── controller
       ├── service
       └── mapper
   ```

3. 添加必要的Maven依赖：
   ```xml
   <dependencies>
       <!-- Spring Boot Web -->
       <dependency>
           <groupId>org.springframework.boot</groupId>
           <artifactId>spring-boot-starter-web</artifactId>
       </dependency>

       <!-- MyBatis -->
       <dependency>
           <groupId>org.mybatis.spring.boot</groupId>
           <artifactId>mybatis-spring-boot-starter</artifactId>
           <version>2.3.1</version>
       </dependency>

       <!-- MySQL Driver -->
       <dependency>
           <groupId>mysql</groupId>
           <artifactId>mysql-connector-java</artifactId>
           <version>8.0.33</version>
       </dependency>

       <!-- Lombok -->
       <dependency>
           <groupId>org.projectlombok</groupId>
           <artifactId>lombok</artifactId>
           <optional>true</optional>
       </dependency>

       <!-- Validation -->
       <dependency>
           <groupId>org.springframework.boot</groupId>
           <artifactId>spring-boot-starter-validation</artifactId>
       </dependency>

       <!-- Swagger (可选) -->
       <dependency>
           <groupId>io.springfox</groupId>
           <artifactId>springfox-boot-starter</artifactId>
           <version>3.0.0</version>
       </dependency>
   </dependencies>
   ```

4. 创建通用类：
   - `Result.java` - 统一响应结果类
   - `PageRequest.java` - 分页请求类
   - `PageResult.java` - 分页结果类

5. 将生成的Java代码粘贴到对应的包中

6. 配置应用程序入口类：
   ```java
   @SpringBootApplication
   @MapperScan({
       "com.student.management.mapper",
       "com.student.management.classes.mapper",
       "com.student.management.teacher.mapper",
       "com.student.management.course.mapper",
       "com.student.management.score.mapper"
   })
   @EnableSwagger2
   public class StudentManagementApplication {
       public static void main(String[] args) {
           SpringApplication.run(StudentManagementApplication.class, args);
       }
   }
   ```

### 11.3 前端部署

1. 创建一个新的Vue项目，建议使用Vue 3：
   ```bash
   npm create vite@latest student-frontend -- --template vue
   cd student-frontend
   ```

2. 安装必要的依赖：
   ```bash
   npm install element-plus axios vue-router echarts
   ```

3. 创建API工具类：
   - 创建 `src/utils/request.js`：
     ```javascript
     import axios from 'axios'
     
     const service = axios.create({
       baseURL: 'http://localhost:8080', // 后端服务地址
       timeout: 10000
     })
     
     // 请求拦截器
     service.interceptors.request.use(
       config => {
         // 可以在这里添加 token 等
         return config
       },
       error => {
         return Promise.reject(error)
       }
     )
     
     // 响应拦截器
     service.interceptors.response.use(
       response => {
         const res = response.data
         if (res.code === 200) {
           return res
         } else {
           return Promise.reject(new Error(res.message || '请求失败'))
         }
       },
       error => {
         return Promise.reject(error)
       }
     )
     
     export default service
     ```

4. 创建API模块：
   - `src/api/student.js` - 学生管理API
   - `src/api/class.js` - 班级管理API
   - `src/api/teacher.js` - 教师管理API
   - `src/api/course.js` - 课程管理API
   - `src/api/score.js` - 成绩管理API

5. 在 `src/views/` 目录下创建相应的文件夹：
   - `src/views/student/`
   - `src/views/class/`
   - `src/views/teacher/`
   - `src/views/course/`
   - `src/views/score/`

6. 将生成的Vue组件文件复制到对应的目录中

7. 配置Vue Router：
   ```javascript
   // src/router/index.js
   import { createRouter, createWebHistory } from 'vue-router'
   
   // 导入学生管理相关组件
   import StudentList from '../views/student/List.vue'
   import StudentForm from '../views/student/Form.vue'
   import StudentDetail from '../views/student/Detail.vue'
   import StudentScores from '../views/student/Scores.vue'
   
   // 导入班级管理相关组件
   import ClassList from '../views/class/List.vue'
   import ClassForm from '../views/class/Form.vue'
   import ClassStudents from '../views/class/Students.vue'
   
   // 导入教师管理相关组件
   import TeacherList from '../views/teacher/List.vue'
   import TeacherForm from '../views/teacher/Form.vue'
   
   // 导入课程管理相关组件
   import CourseList from '../views/course/List.vue'
   import CourseForm from '../views/course/Form.vue'
   import CourseScores from '../views/course/Scores.vue'
   
   // 导入成绩管理相关组件
   import ScoreList from '../views/score/List.vue'
   import ScoreForm from '../views/score/Form.vue'
   import ScoreBatchInput from '../views/score/BatchInput.vue'
   import ScoreStatistics from '../views/score/Statistics.vue'
   import ScoreExport from '../views/score/Export.vue'
   
   const routes = [
     {
       path: '/',
       redirect: '/student/list'
     },
     // 学生管理路由
     {
       path: '/student/list',
       name: 'StudentList',
       component: StudentList
     },
     {
       path: '/student/form/:id?',
       name: 'StudentForm',
       component: StudentForm
     },
     {
       path: '/student/detail/:id',
       name: 'StudentDetail',
       component: StudentDetail
     },
     {
       path: '/student/scores/:id',
       name: 'StudentScores',
       component: StudentScores
     },
     // 班级管理路由
     {
       path: '/class/list',
       name: 'ClassList',
       component: ClassList
     },
     {
       path: '/class/form/:id?',
       name: 'ClassForm',
       component: ClassForm
     },
     {
       path: '/class/students/:id',
       name: 'ClassStudents',
       component: ClassStudents
     },
     // 教师管理路由
     {
       path: '/teacher/list',
       name: 'TeacherList',
       component: TeacherList
     },
     {
       path: '/teacher/form/:id?',
       name: 'TeacherForm',
       component: TeacherForm
     },
     // 课程管理路由
     {
       path: '/course/list',
       name: 'CourseList',
       component: CourseList
     },
     {
       path: '/course/form/:id?',
       name: 'CourseForm',
       component: CourseForm
     },
     {
       path: '/course/scores/:id',
       name: 'CourseScores',
       component: CourseScores
     },
     // 成绩管理路由
     {
       path: '/score/list',
       name: 'ScoreList',
       component: ScoreList
     },
     {
       path: '/score/form/:id?',
       name: 'ScoreForm',
       component: ScoreForm
     },
     {
       path: '/score/batchInput',
       name: 'ScoreBatchInput',
       component: ScoreBatchInput
     },
     {
       path: '/score/statistics',
       name: 'ScoreStatistics',
       component: ScoreStatistics
     },
     {
       path: '/score/export',
       name: 'ScoreExport',
       component: ScoreExport
     }
   ]
   
   const router = createRouter({
     history: createWebHistory(process.env.BASE_URL),
     routes
   })
   
   export default router
   ```

8. 配置Element Plus：
   ```javascript
   // src/main.js
   import { createApp } from 'vue'
   import ElementPlus from 'element-plus'
   import 'element-plus/dist/index.css'
   import App from './App.vue'
   import router from './router'
   
   const app = createApp(App)
   
   app.use(ElementPlus)
   app.use(router)
   
   app.mount('#app')
   ```

### 11.4 启动应用

1. 启动后端服务：
   ```bash
   # 在后端项目目录
   mvn spring-boot:run
   ```

2. 启动前端服务：
   ```bash
   # 在前端项目目录
   npm run dev
   ```

3. 访问学生管理系统：
   - 前端地址：`http://localhost:5173/`（根据vite启动时的输出调整）
   - 学生列表页面：`http://localhost:5173/student/list`
   - 班级列表页面：`http://localhost:5173/class/list`
   - 课程列表页面：`http://localhost:5173/course/list`
   - 成绩列表页面：`http://localhost:5173/score/list`

## 第十二步：系统功能测试

### 12.1 基础功能测试

1. **学生管理功能测试**：
   - 添加新学生，验证必填字段和格式验证
   - 编辑学生信息，验证更新功能
   - 删除学生，验证删除功能
   - 搜索和筛选学生，验证查询功能
   - 查看学生详情，验证关联信息显示

2. **班级管理功能测试**：
   - 添加新班级，验证班主任选择
   - 编辑班级信息，验证更新功能
   - 删除班级，验证删除功能
   - 查看班级学生列表，验证一对多关系

3. **课程管理功能测试**：
   - 添加新课程，验证课程信息
   - 编辑课程信息，验证更新功能
   - 删除课程，验证删除功能
   - 查看课程详情，验证授课教师信息

4. **成绩管理功能测试**：
   - 录入学生成绩，验证唯一性约束
   - 批量导入成绩，验证批量操作功能
   - 统计成绩数据，验证图表显示
   - 导出成绩单，验证文件格式

### 12.2 关联功能测试

1. **学生-班级关联测试**：
   - 在学生详情页查看班级信息
   - 在班级详情页查看学生列表
   - 修改学生所属班级，验证更新

2. **课程-成绩关联测试**：
   - 在课程详情页查看成绩列表
   - 按课程统计成绩分布

3. **学生-成绩关联测试**：
   - 在学生详情页查看成绩列表
   - 计算学生平均成绩

## 重要注意事项

### 1. 权限管理

- 生产环境中请修改默认密码（admin/123456）为强密码
- 建议配置适当的用户权限，限制不同角色的操作范围
- 定期备份数据库，防止数据丢失

### 2. 系统配置

- 确保数据库连接信息正确配置，特别是用户名和密码
- 生产环境中建议启用HTTPS
- 调整合适的JVM参数以优化性能
  ```bash
  java -Xms512m -Xmx1024m -jar target/student-management-1.0.0.jar
  ```

### 3. 数据安全

- 敏感数据（如学生身份证号）应进行加密存储
- 定期清理操作日志，避免数据库过大
- 实现数据备份和恢复策略

### 4. 性能优化

- 为数据库表的外键和常用查询字段添加索引
- 对于大数据量的查询，考虑使用分页和缓存
- 优化批量操作性能，避免频繁数据库交互

## 系统扩展建议

1. **集成Redis缓存**：提高系统响应速度
2. **添加考勤管理功能**：记录学生出勤情况
3. **实现家校互动平台**：家长可以查看学生成绩和表现
4. **添加宿舍管理模块**：管理学生住宿信息
5. **集成在线考试系统**：支持在线答题和自动评分
6. **添加数据分析功能**：基于大数据分析学生学习情况

通过上述扩展，可以使学生管理系统更加完善，满足更多复杂场景的需求。

## 总结

本指南详细介绍了如何使用元数据管理系统构建一个完整的学生管理系统，包含学生、班级、课程、成绩等核心功能模块。通过元数据配置和代码生成，您可以快速实现一个功能完善、结构清晰的学生管理系统。

系统的核心特点包括：
1. 完整的模块结构设计，包含学生、班级、课程、成绩等核心模块
2. 完善的表关联关系配置，支持一对多、多对一等关系
3. 丰富的功能节点配置，满足不同业务场景需求
4. 自动代码生成，快速实现前后端功能
5. 灵活的业务规则配置，支持数据验证和业务约束

通过本指南，您可以快速上手元数据管理系统，构建符合业务需求的学生管理系统，并根据实际需要进行功能扩展和定制化开发。

祝您使用愉快！