# 学生管理系统实现指南（完整版）

本指南详细说明如何使用元数据管理系统快速构建一个功能完整的学生管理系统，包含学生、班级、课程、成绩等核心管理功能。通过元数据配置和代码生成，您可以在不编写大量重复代码的情况下，实现完整的业务功能。

## 系统概述

### 元数据管理系统介绍

当前项目是一个功能强大的元数据管理系统，它允许用户通过图形化界面定义完整的业务模型，包括模块类型、模块结构、功能节点、表结构、字段配置、表关联关系等，然后自动生成完整的业务代码，包括：
- 数据库建表SQL
- Java后端代码（Entity、Controller、Service、Mapper等）
- Vue前端页面（列表页、表单页、详情页等）
- 路由配置和权限设置

系统采用模块化设计，支持复杂业务场景的快速构建和定制。核心技术栈包括：
- 后端：Spring Boot 2.7.18, MyBatis, MySQL 8.0, Freemarker
- 前端：Vue 3, Element Plus, Vue Router, Axios

### 学生管理系统目标

通过配置元数据，我们将实现一个功能完整的学生管理系统，包括：
- 学生信息的基础管理（增删改查）
- 班级信息的管理
- 课程信息的管理
- 成绩录入和统计分析
- 完整的表关联关系配置和使用
- 完整的前后端代码生成和部署

本指南将详细介绍如何利用元数据管理系统的各种功能，快速构建和定制学生管理系统。

## 环境准备

### 1. 运行元数据管理系统

1. 确保已安装以下环境：
   - JDK 17（必须，系统依赖Java 17）
   - Maven 3.6+ 
   - Node.js 14+ 和 npm 6+
   - MySQL 8.0 数据库

2. 运行数据库初始化脚本创建元数据数据库：
   ```bash
   # 在MySQL命令行中执行
   source d:/Java/meta-management/database/init.sql
   ```
   该脚本会创建`metadata_db`数据库及相关表结构，并初始化默认管理员账号。

3. 配置数据库连接（可选，如需修改默认配置）：
   编辑`src/main/resources/application.properties`文件，修改数据库连接信息。

4. 启动后端服务：
   ```bash
   # 在项目根目录执行
   mvn spring-boot:run
   ```
   或打包后运行：
   ```bash
   mvn package
   java -jar target/metadata-system-1.0.0.war
   ```

5. 启动前端服务：
   ```bash
   # 进入frontend目录
   cd frontend
   npm install
   npm run serve
   ```

6. 访问系统：
   - 后端API地址：`http://localhost:8080/metadata-system/api/`
   - 前端访问地址：`http://localhost:8080/metadata-system/`
   - 登录账号：admin
   - 登录密码：123456

### 2. 环境要求

- 后端服务已启动，基于Spring Boot 2.7.18
- 前端服务已启动，基于Vue 3
- MySQL 8.0 数据库已配置

系统初始化时已创建了`metadata_db`数据库及相关表结构，并初始化了默认管理员账号和基础模块类型。

## 实现步骤

### 前置准备：业务系统管理

业务系统是元数据管理的顶层概念，用于组织和管理不同的业务应用。在创建模块和表结构之前，我们需要先创建一个业务系统，用于关联后续创建的所有模块和表。

#### 1. 创建业务系统
1. 登录系统后，在左侧菜单中点击 **业务系统管理**
2. 点击页面顶部的 **新增系统** 按钮
3. 在对话框中填写以下信息：
   - **业务编码**：`STUDENT_SYSTEM`
   - **业务名称**：`学生管理系统`
   - **描述**：`完整的学生管理系统，包含学生、班级、课程、成绩等核心功能`
   - **是否默认**：是
4. 点击 **保存** 按钮

#### 2. 关联模块到业务系统

在创建完所有模块后，需要将模块关联到业务系统，以便在代码生成时使用。

1. 在 **业务系统管理** 页面，找到已创建的 `学生管理系统`
2. 点击 **关联模块** 按钮
3. 在弹出的对话框中，选择需要关联的模块：
   - `STUDENT_MODULE`（学生管理）
   - `CLASS_MODULE`（班级管理）
   - `TEACHER_MODULE`（教师管理）
   - `COURSE_MODULE`（课程管理）
   - `SCORE_MODULE`（成绩管理）
4. 点击 **保存** 按钮

#### 3. 业务系统的作用

- 组织和管理不同的业务模块
- 在代码生成时，作为代码包的命名空间和组织单位
- 方便对不同业务系统进行权限管理和资源隔离
- 支持多业务系统的并行开发和部署

### 模块类型管理

在创建模块之前，需要了解模块类型的管理。模块类型用于组织和分类不同功能模块，决定了模块的基础功能和行为。系统初始化时已创建了几种常用的模块类型，您也可以根据需要自定义新的模块类型。

#### 1. 查看现有模块类型
1. 登录系统后，在左侧菜单中点击顶部的 **模块类型管理**
2. 系统将显示所有已配置的模块类型列表，包括系统初始化时创建的模块类型：
   - DATA_MANAGE：数据管理型，支持标准CRUD操作
   - PROCESS_APPROVE：流程审批型，支持流程审批功能
   - STAT_REPORT：统计报表型，支持统计报表功能
   - BATCH_OPERATE：批量操作型，支持批量导入导出功能

#### 2. 创建新模块类型
根据架构设计，我们需要创建一个新的模块类型用于成绩管理：
1. 在模块类型管理页面点击 **新增类型** 按钮，会弹出一个「新增类型」对话框
2. 在对话框中填写以下信息：
   - **类型编码**：`SCORE_MANAGE`
   - **类型名称**：`成绩管理型`
   - **默认功能节点**：`LIST_PAGE,FORM_PAGE,REPORT_PAGE,BATCH_IMPORT_PAGE,BATCH_EXPORT_PAGE`
   - **描述**：`用于成绩录入、统计和管理的模块类型`
3. 点击 **保存** 按钮

> **注意**：
> - 当前系统支持的默认节点类型有：LIST_PAGE、FORM_PAGE、DETAIL_PAGE、PROCESS_PAGE、REPORT_PAGE、BATCH_IMPORT_PAGE、BATCH_EXPORT_PAGE
> - 系统会根据默认节点类型自动创建相应的功能节点
> - CUSTOM_PAGE 类型的节点需要手动创建，不能作为默认节点类型

### 第一步：创建基础信息管理模块

#### 1. 创建学生管理模块
1. 点击左侧菜单的 **模块管理**
2. 点击页面顶部的 **新增模块** 按钮
3. 填写模块信息：
   - **模块编码**：`STUDENT_MODULE`（必须唯一）
   - **模块名称**：`学生管理`
   - **模块类型**：选择 `DATA_MANAGE`（数据管理型）
   - **描述**：`学生信息管理模块，支持学生信息的增删改查操作`
   - **排序**：1
   - **图标**：`el-icon-user`
   - **路由路径**：`/student`
   - **组件路径**：`views/student/index`
4. 点击 **保存** 按钮

#### 2. 创建班级管理模块
1. 点击页面顶部的 **新增模块** 按钮
2. 填写模块信息：
   - **模块编码**：`CLASS_MODULE`
   - **模块名称**：`班级管理`
   - **模块类型**：选择 `DATA_MANAGE`
   - **描述**：`班级信息管理模块`
   - **排序**：2
   - **图标**：`el-icon-office-building`
   - **路由路径**：`/class`
   - **组件路径**：`views/class/index`
3. 点击 **保存** 按钮

#### 3. 创建教师管理模块
1. 点击页面顶部的 **新增模块** 按钮
2. 填写模块信息：
   - **模块编码**：`TEACHER_MODULE`
   - **模块名称**：`教师管理`
   - **模块类型**：选择 `DATA_MANAGE`
   - **描述**：`教师信息管理模块`
   - **排序**：3
   - **图标**：`el-icon-s-custom`
   - **路由路径**：`/teacher`
   - **组件路径**：`views/teacher/index`
3. 点击 **保存** 按钮

### 第二步：创建课程管理模块

1. 点击页面顶部的 **新增模块** 按钮
2. 填写模块信息：
   - **模块编码**：`COURSE_MODULE`
   - **模块名称**：`课程管理`
   - **模块类型**：选择 `DATA_MANAGE`
   - **描述**：`课程信息管理模块，支持课程信息的增删改查和成绩管理`
   - **排序**：4
   - **图标**：`el-icon-edit-outline`
   - **路由路径**：`/course`
   - **组件路径**：`views/course/index`
3. 点击 **保存** 按钮

### 第三步：创建成绩管理模块

1. 点击页面顶部的 **新增模块** 按钮
2. 填写模块信息：
   - **模块编码**：`SCORE_MODULE`
   - **模块名称**：`成绩管理`
   - **模块类型**：选择 `SCORE_MANAGE`
   - **描述**：`成绩管理模块，支持成绩录入、统计和分析`
   - **排序**：5
   - **图标**：`el-icon-data-line`
   - **路由路径**：`/score`
   - **组件路径**：`views/score/index`
3. 点击 **保存** 按钮

### 第四步：创建表结构

#### 1. 创建班级表
1. 点击左侧菜单的 **表管理**
2. 点击 **新增表** 按钮
3. 填写表信息：
   - **表编码**：`CLASS_TABLE`
   - **表名称**：`班级信息表`
   - **主键策略**：`AUTO`（自增）
   - **描述**：`存储班级基本信息`
4. 点击 **保存** 按钮

#### 2. 创建教师表
1. 点击 **新增表** 按钮
2. 填写表信息：
   - **表编码**：`TEACHER_TABLE`
   - **表名称**：`教师信息表`
   - **主键策略**：`AUTO`（自增）
   - **描述**：`存储教师基本信息`
3. 点击 **保存** 按钮

#### 3. 创建学生表
1. 点击 **新增表** 按钮
2. 填写表信息：
   - **表编码**：`STUDENT_TABLE`
   - **表名称**：`学生信息表`
   - **主键策略**：`AUTO`（自增）
   - **描述**：`存储学生基本信息`
3. 点击 **保存** 按钮

#### 4. 创建课程表
1. 点击 **新增表** 按钮
2. 填写表信息：
   - **表编码**：`COURSE_TABLE`
   - **表名称**：`课程信息表`
   - **主键策略**：`AUTO`（自增）
   - **描述**：`存储课程基本信息`
3. 点击 **保存** 按钮

#### 5. 创建成绩表
1. 点击 **新增表** 按钮
2. 填写表信息：
   - **表编码**：`SCORE_TABLE`
   - **表名称**：`成绩信息表`
   - **主键策略**：`AUTO`（自增）
   - **描述**：`存储学生课程成绩信息`
3. 点击 **保存** 按钮

### 第五步：配置表字段

#### 1. 配置班级表字段
1. 点击左侧菜单的 **字段管理**
2. 在表选择下拉框中选择 `CLASS_TABLE`
3. 点击 **新增字段** 按钮，依次添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 | 校验规则 |
|---------|---------|---------|--------|---------|---------|------|----------|
| CLASS_CODE | class_code | varchar(50) | 班级编号 | 是 | input | 1 | `{"pattern": "^\\w{4,20}$", "message": "班级编号只能包含字母数字下划线，长度4-20位"}` |
| CLASS_NAME | class_name | varchar(100) | 班级名称 | 是 | input | 2 | |
| GRADE | grade | varchar(50) | 年级 | 否 | input | 3 | |
| HEAD_TEACHER_ID | head_teacher_id | bigint | 班主任ID | 否 | select | 4 | |
| STUDENT_COUNT | student_count | int | 学生人数 | 否 | number | 5 | `{"min": 0, "message": "学生人数不能为负数"}` |

#### 2. 配置教师表字段
1. 在表选择下拉框中选择 `TEACHER_TABLE`
2. 点击 **新增字段** 按钮，依次添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 | 校验规则 |
|---------|---------|---------|--------|---------|---------|------|----------|
| TEACHER_CODE | teacher_code | varchar(50) | 教师编号 | 是 | input | 1 | `{"pattern": "^\\w{4,20}$", "message": "教师编号只能包含字母数字下划线，长度4-20位"}` |
| TEACHER_NAME | teacher_name | varchar(100) | 教师姓名 | 是 | input | 2 | |
| TEACHER_GENDER | gender | varchar(10) | 性别 | 否 | select | 3 | `{"options": ["男", "女"]}` |
| TEACHER_TITLE | title | varchar(50) | 职称 | 否 | input | 4 | |
| TEACHER_PHONE | phone | varchar(20) | 手机号 | 否 | input | 5 | `{"pattern": "^1[3-9]\\d{9}$", "message": "请输入正确的手机号"}` |
| TEACHER_EMAIL | email | varchar(100) | 邮箱 | 否 | input | 6 | `{"pattern": "^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$", "message": "请输入正确的邮箱地址"}` |

#### 3. 配置学生表字段
1. 在表选择下拉框中选择 `STUDENT_TABLE`
2. 点击 **新增字段** 按钮，依次添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 | 校验规则 |
|---------|---------|---------|--------|---------|---------|------|----------|
| STU_CODE | student_code | varchar(50) | 学号 | 是 | input | 1 | `{"pattern": "^\\d{10}$", "message": "学号必须为10位数字"}` |
| STU_NAME | student_name | varchar(100) | 姓名 | 是 | input | 2 | |
| STU_GENDER | gender | varchar(10) | 性别 | 是 | select | 3 | `{"options": ["男", "女"]}` |
| STU_AGE | age | int | 年龄 | 否 | number | 4 | `{"min": 1, "max": 100, "message": "年龄必须在1-100之间"}` |
| STU_PHONE | phone | varchar(20) | 手机号 | 否 | input | 5 | `{"pattern": "^1[3-9]\\d{9}$", "message": "请输入正确的手机号"}` |
| STU_EMAIL | email | varchar(100) | 邮箱 | 否 | input | 6 | `{"pattern": "^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$", "message": "请输入正确的邮箱地址"}` |
| CLASS_ID | class_id | bigint | 班级ID | 否 | select | 7 | |
| STU_ENTRY_DATE | entry_date | date | 入学日期 | 否 | datepicker | 8 | |
| STU_ADDRESS | address | varchar(200) | 地址 | 否 | input | 9 | |
| STU_STATUS | status | varchar(20) | 状态 | 否 | select | 10 | `{"options": ["在读", "休学", "毕业", "退学"]}` |


#### 4. 配置课程表字段
1. 在表选择下拉框中选择 `COURSE_TABLE`
2. 点击 **新增字段** 按钮，依次添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 | 校验规则 |
|---------|---------|---------|--------|---------|---------|------|----------|
| COURSE_CODE | course_code | varchar(50) | 课程编号 | 是 | input | 1 | `{"pattern": "^\\w{4,20}$", "message": "课程编号只能包含字母数字下划线，长度4-20位"}` |
| COURSE_NAME | course_name | varchar(100) | 课程名称 | 是 | input | 2 | |
| COURSE_TYPE | course_type | varchar(50) | 课程类型 | 否 | select | 3 | `{"options": ["必修课", "选修课"]}` |
| CREDITS | credits | decimal(4,1) | 学分 | 否 | number | 4 | `{"min": 0, "max": 10, "message": "学分必须在0-10之间"}` |
| HOURS | hours | int | 课时 | 否 | number | 5 | `{"min": 0, "message": "课时不能为负数"}` |
| COURSE_DESCRIPTION | description | text | 课程描述 | 否 | textarea | 6 | |
| TEACHER_ID | teacher_id | bigint | 授课教师ID | 否 | select | 7 | |

#### 5. 配置成绩表字段
1. 在表选择下拉框中选择 `SCORE_TABLE`
2. 点击 **新增字段** 按钮，依次添加以下字段：

| 字段编码       | 字段名称       | 字段类型         | 显示名  | 是否必填 | 表单组件       | 排序 | 校验规则                                                |
|------------|------------|--------------|------|------|------------|----|-----------------------------------------------------|
| STUDENT_ID | student_id | bigint       | 学生ID | 是    | select     | 1  |                                                     |
| COURSE_ID  | course_id  | bigint       | 课程ID | 是    | select     | 2  |                                                     |
| SCORE      | score      | decimal(5,2) | 分数   | 否    | number     | 3  | `{"min": 0, "max": 100, "message": "分数必须在0-100之间"}` |
| EXAM_DATE  | exam_date  | date         | 考试日期 | 否    | datepicker | 4  |                                                     |
| SEMESTER   | semester   | varchar(50)  | 学期   | 否    | input      | 5  |                                                     |
| REMARK     | remark     | text         | 备注   | 否    | textarea   | 6  |                                                     |

### 第六步：配置表关联关系

> **注意**：系统会根据选择的主键策略自动创建主键字段：
> - 当主键策略为`AUTO`（自增）时，会创建名为`id`的BIGINT类型字段
> - 当主键策略为`UUID`时，会创建名为`uuid`的VARCHAR(36)类型字段
> 
> 本指南中所有表均使用`id`作为主键字段（自增策略），用于表关联关系配置。

#### 1. 配置教师和班级的一对多关系
1. 点击左侧菜单的 **表关联关系管理**
2. 点击 **新增关联** 按钮
3. 配置教师-班级关联：
   - **关联编码**：`TEACHER_CLASS_REL`
   - **关联名称**：`教师_班级`
   - **主表**：选择 `TEACHER_TABLE`
   - **主表关联字段**：`id`
   - **从表**：选择 `CLASS_TABLE`
   - **从表外键字段**：`HEAD_TEACHER_ID`
   - **关联类型**：选择 `一对多`
   - **创建外键约束**：根据需要选择是否同时创建数据库外键约束
   - **描述**：`一个教师可以担任多个班级的班主任`
4. 点击 **确定** 按钮

#### 2. 配置班级和学生的一对多关系
1. 点击 **新增关联** 按钮
2. 配置班级-学生关联：
   - **关联编码**：`CLASS_STUDENT_REL`
   - **关联名称**：`班级_学生`
   - **主表**：选择 `CLASS_TABLE`
   - **主表关联字段**：`id`
   - **从表**：选择 `STUDENT_TABLE`
   - **从表外键字段**：`CLASS_ID`
   - **关联类型**：选择 `一对多`
   - **创建外键约束**：根据需要选择是否同时创建数据库外键约束
   - **描述**：`一个班级包含多个学生`
3. 点击 **确定** 按钮

#### 3. 配置学生和班级的多对一关系
1. 点击 **新增关联** 按钮
2. 配置学生-班级关联：
   - **关联编码**：`STUDENT_CLASS_REL`
   - **关联名称**：`学生_班级`
   - **主表**：选择 `STUDENT_TABLE`
   - **主表关联字段**：`CLASS_ID`
   - **从表**：选择 `CLASS_TABLE`
   - **从表外键字段**：`id`
   - **关联类型**：选择 `多对一`
   - **创建外键约束**：根据需要选择是否同时创建数据库外键约束
   - **描述**：`多个学生属于一个班级`
3. 点击 **确定** 按钮

#### 4. 配置教师和课程的一对多关系
1. 点击 **新增关联** 按钮
2. 配置教师-课程关联：
   - **关联编码**：`TEACHER_COURSE_REL`
   - **关联名称**：`教师_课程`
   - **主表**：选择 `TEACHER_TABLE`
   - **主表关联字段**：`id`
   - **从表**：选择 `COURSE_TABLE`
   - **从表外键字段**：`TEACHER_ID`
   - **关联类型**：选择 `一对多`
   - **创建外键约束**：根据需要选择是否同时创建数据库外键约束
   - **描述**：`一个教师可以教授多门课程`
3. 点击 **确定** 按钮

#### 5. 配置课程和学生成绩的一对多关系
1. 点击 **新增关联** 按钮
2. 配置课程-成绩关联：
   - **关联编码**：`COURSE_SCORE_REL`
   - **关联名称**：`课程_成绩`
   - **主表**：选择 `COURSE_TABLE`
   - **主表关联字段**：`id`
   - **从表**：选择 `SCORE_TABLE`
   - **从表外键字段**：`COURSE_ID`
   - **关联类型**：选择 `一对多`
   - **创建外键约束**：根据需要选择是否同时创建数据库外键约束
   - **描述**：`一门课程有多个学生成绩`
3. 点击 **确定** 按钮

#### 6. 配置学生和成绩的一对多关系
1. 点击 **新增关联** 按钮
2. 配置学生-成绩关联：
   - **关联编码**：`STUDENT_SCORE_REL`
   - **关联名称**：`学生_成绩`
   - **主表**：选择 `STUDENT_TABLE`
   - **主表关联字段**：`id`
   - **从表**：选择 `SCORE_TABLE`
   - **从表外键字段**：`STUDENT_ID`
   - **关联类型**：选择 `一对多`
   - **创建外键约束**：根据需要选择是否同时创建数据库外键约束
   - **描述**：`一个学生有多门课程成绩`
3. 点击 **确定** 按钮

### 第七步：关联模块和表

#### 1. 关联学生管理模块和学生表
1. 在 **模块管理** 页面，点击 `STUDENT_MODULE` 模块的 **编辑** 按钮
2. 在弹出的表单中，找到 **关联表** 部分
3. 点击添加按钮，选择 `STUDENT_TABLE`
4. 点击 **保存** 按钮

#### 2. 关联班级管理模块和班级表
1. 在 **模块管理** 页面，点击 `CLASS_MODULE` 模块的 **编辑** 按钮
2. 在弹出的表单中，找到 **关联表** 部分
3. 点击添加按钮，选择 `CLASS_TABLE`
4. 点击 **保存** 按钮

#### 3. 关联教师管理模块和教师表
1. 在 **模块管理** 页面，点击 `TEACHER_MODULE` 模块的 **编辑** 按钮
2. 在弹出的表单中，找到 **关联表** 部分
3. 点击添加按钮，选择 `TEACHER_TABLE`
4. 点击 **保存** 按钮

#### 4. 关联课程管理模块和课程表
1. 在 **模块管理** 页面，点击 `COURSE_MODULE` 模块的 **编辑** 按钮
2. 在弹出的表单中，找到 **关联表** 部分
3. 点击添加按钮，选择 `COURSE_TABLE`
4. 点击 **保存** 按钮

#### 5. 关联成绩管理模块和成绩表
1. 在 **模块管理** 页面，点击 `SCORE_MODULE` 模块的 **编辑** 按钮
2. 在弹出的表单中，找到 **关联表** 部分
3. 点击添加按钮，选择 `SCORE_TABLE`
4. 点击 **保存** 按钮

### 第八步：配置功能节点

> **创建功能节点的前置条件**：
> 1. 已创建**业务系统**和**模块**
> 2. 已创建**表结构**
> 3. 已将**表关联到模块**（第七步操作）
> 4. 功能节点是**关联到表**的，用于实现该表的业务功能

#### 功能节点类型说明

在配置功能节点之前，需要了解系统支持的节点类型及其用途：

| 节点类型 | 节点名称 | 用途说明 |
|---------|---------|----------|
| LIST_PAGE | 列表页 | 用于展示数据列表，支持查询、筛选、分页等功能 |
| FORM_PAGE | 表单页 | 用于数据的新增和编辑操作 |
| DETAIL_PAGE | 详情页 | 用于展示单条数据的详细信息 |
| CUSTOM_PAGE | 自定义页面 | 用于实现特定的业务逻辑和界面，如成绩管理、班级学生管理等 |
| PROCESS_PAGE | 流程流转页 | 用于流程审批和流转场景 |
| REPORT_PAGE | 报表展示页 | 用于数据统计和图表展示 |
| BATCH_IMPORT_PAGE | 批量导入页 | 用于批量导入数据 |
| BATCH_EXPORT_PAGE | 批量导出页 | 用于批量导出数据 |

#### 功能节点管理页面介绍

1. 点击左侧菜单的 **功能节点管理**，进入功能节点管理页面
2. 页面顶部包含两个重要的选择器：
   - **业务系统选择器**：用于筛选当前业务系统下的模块和节点
   - **模块选择器**：用于筛选当前模块下的功能节点，新增节点时会默认使用此选择的模块
3. 页面操作按钮：
   - **新增节点**：用于创建新的功能节点
   - **批量删除**：用于删除多个功能节点

> **注意**：
> - 只有已关联到模块的表，才能在创建功能节点时被选择
> - 一个表可以关联多个不同类型的功能节点
> - 功能节点是实现业务功能的核心配置，直接影响代码生成的结果

#### 1. 配置学生管理模块功能节点

> **节点类型说明**：
> - 系统会根据模块类型的默认节点自动创建基础功能节点
> - `CUSTOM_PAGE` 类型的节点需要**手动创建**，用于实现特定业务逻辑
> - 每个模块下可以创建多个不同类型的功能节点

##### 1.1 学生列表页节点
1. 在功能节点管理页面顶部的 **业务系统选择器** 中选择 `STUDENT_SYSTEM`
2. 在 **模块选择器** 中选择 `STUDENT_MODULE`
3. 点击 **新增节点** 按钮
4. 配置列表页节点：
   - **节点编码**：`STUDENT_LIST`
   - **节点名称**：`学生列表页`
   - **节点类型**：`LIST_PAGE`
   - **关联表**：`STUDENT_TABLE`
   - **路由路径**：`/student/list`
   - **组件路径**：`views/student/List.vue`
   - **排序**：1
   - **图标**：`el-icon-document`
   - **是否在菜单显示**：是
5. 点击 **保存** 按钮

##### 1.2 学生表单页节点
1. 确保页面顶部的 **模块选择器** 仍为 `STUDENT_MODULE`
2. 点击 **新增节点** 按钮
3. 配置表单页节点：
   - **节点编码**：`STUDENT_FORM`
   - **节点名称**：`学生表单页`
   - **节点类型**：`FORM_PAGE`
   - **关联表**：`STUDENT_TABLE`
   - **路由路径**：`/student/form/:id?`
   - **组件路径**：`views/student/Form.vue`
   - **排序**：2
   - **是否在菜单显示**：否
4. 点击 **保存** 按钮

##### 1.3 学生详情页节点
1. 确保页面顶部的 **模块选择器** 仍为 `STUDENT_MODULE`
2. 点击 **新增节点** 按钮
3. 配置详情页节点：
   - **节点编码**：`STUDENT_DETAIL`
   - **节点名称**：`学生详情页`
   - **节点类型**：`DETAIL_PAGE`
   - **关联表**：`STUDENT_TABLE`
   - **路由路径**：`/student/detail/:id`
   - **组件路径**：`views/student/Detail.vue`
   - **排序**：3
   - **是否在菜单显示**：否
4. 点击 **保存** 按钮

##### 1.4 学生成绩管理节点
1. 确保页面顶部的 **模块选择器** 仍为 `STUDENT_MODULE`
2. 点击 **新增节点** 按钮
3. 配置成绩管理节点：
   - **节点编码**：`STUDENT_SCORE`
   - **节点名称**：`学生成绩管理`
   - **节点类型**：`CUSTOM_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/student/scores/:id`
   - **组件路径**：`views/student/Scores.vue`
   - **排序**：4
   - **是否在菜单显示**：否
4. 点击 **保存** 按钮

#### 2. 配置班级管理模块功能节点

##### 2.1 班级列表页节点
1. 在页面顶部的 **模块选择器** 中选择 `CLASS_MODULE`
2. 点击 **新增节点** 按钮
3. 配置列表页节点：
   - **节点编码**：`CLASS_LIST`
   - **节点名称**：`班级列表页`
   - **节点类型**：`LIST_PAGE`
   - **关联表**：`CLASS_TABLE`
   - **路由路径**：`/class/list`
   - **组件路径**：`views/class/List.vue`
   - **排序**：1
   - **图标**：`el-icon-document`
   - **是否在菜单显示**：是
4. 点击 **保存** 按钮

##### 2.2 班级表单页节点
1. 确保页面顶部的 **模块选择器** 仍为 `CLASS_MODULE`
2. 点击 **新增节点** 按钮
3. 配置表单页节点：
   - **节点编码**：`CLASS_FORM`
   - **节点名称**：`班级表单页`
   - **节点类型**：`FORM_PAGE`
   - **关联表**：`CLASS_TABLE`
   - **路由路径**：`/class/form/:id?`
   - **组件路径**：`views/class/Form.vue`
   - **排序**：2
   - **是否在菜单显示**：否
4. 点击 **保存** 按钮

##### 2.3 班级学生管理节点
1. 确保页面顶部的 **模块选择器** 仍为 `CLASS_MODULE`
2. 点击 **新增节点** 按钮
3. 配置学生管理节点：
   - **节点编码**：`CLASS_STUDENTS`
   - **节点名称**：`班级学生管理`
   - **节点类型**：`CUSTOM_PAGE`
   - **关联表**：`STUDENT_TABLE`
   - **路由路径**：`/class/students/:id`
   - **组件路径**：`views/class/Students.vue`
   - **排序**：3
   - **是否在菜单显示**：否
4. 点击 **保存** 按钮

#### 3. 配置课程管理模块功能节点

##### 3.1 课程列表页节点
1. 在页面顶部的 **模块选择器** 中选择 `COURSE_MODULE`
2. 点击 **新增节点** 按钮
3. 配置列表页节点：
   - **节点编码**：`COURSE_LIST`
   - **节点名称**：`课程列表页`
   - **节点类型**：`LIST_PAGE`
   - **关联表**：`COURSE_TABLE`
   - **路由路径**：`/course/list`
   - **组件路径**：`views/course/List.vue`
   - **排序**：1
   - **图标**：`el-icon-document`
   - **是否在菜单显示**：是
4. 点击 **保存** 按钮

##### 3.2 课程表单页节点
1. 确保页面顶部的 **模块选择器** 仍为 `COURSE_MODULE`
2. 点击 **新增节点** 按钮
3. 配置表单页节点：
   - **节点编码**：`COURSE_FORM`
   - **节点名称**：`课程表单页`
   - **节点类型**：`FORM_PAGE`
   - **关联表**：`COURSE_TABLE`
   - **路由路径**：`/course/form/:id?`
   - **组件路径**：`views/course/Form.vue`
   - **排序**：2
   - **是否在菜单显示**：否
4. 点击 **保存** 按钮

##### 3.3 课程成绩管理节点
1. 确保页面顶部的 **模块选择器** 仍为 `COURSE_MODULE`
2. 点击 **新增节点** 按钮
3. 配置成绩管理节点：
   - **节点编码**：`COURSE_SCORES`
   - **节点名称**：`课程成绩管理`
   - **节点类型**：`CUSTOM_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/course/scores/:id`
   - **组件路径**：`views/course/Scores.vue`
   - **排序**：3
   - **是否在菜单显示**：否
4. 点击 **保存** 按钮

#### 4. 配置成绩管理模块功能节点

##### 4.1 成绩列表页节点
1. 在页面顶部的 **模块选择器** 中选择 `SCORE_MODULE`
2. 点击 **新增节点** 按钮
3. 配置列表页节点：
   - **节点编码**：`SCORE_LIST`
   - **节点名称**：`成绩列表页`
   - **节点类型**：`LIST_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/score/list`
   - **组件路径**：`views/score/List.vue`
   - **排序**：1
   - **图标**：`el-icon-document`
   - **是否在菜单显示**：是
4. 点击 **保存** 按钮

##### 4.2 成绩表单页节点
1. 确保页面顶部的 **模块选择器** 仍为 `SCORE_MODULE`
2. 点击 **新增节点** 按钮
3. 配置表单页节点：
   - **节点编码**：`SCORE_FORM`
   - **节点名称**：`成绩表单页`
   - **节点类型**：`FORM_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/score/form/:id?`
   - **组件路径**：`views/score/Form.vue`
   - **排序**：2
   - **是否在菜单显示**：否
4. 点击 **保存** 按钮

##### 4.3 成绩批量录入节点
1. 确保页面顶部的 **模块选择器** 仍为 `SCORE_MODULE`
2. 点击 **新增节点** 按钮
3. 配置批量录入节点：
   - **节点编码**：`SCORE_BATCH_INPUT`
   - **节点名称**：`成绩批量录入`
   - **节点类型**：`CUSTOM_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/score/batchInput`
   - **组件路径**：`views/score/BatchInput.vue`
   - **排序**：3
   - **图标**：`el-icon-upload`
   - **是否在菜单显示**：是
4. 点击 **保存** 按钮

##### 4.4 成绩统计分析节点
1. 确保页面顶部的 **模块选择器** 仍为 `SCORE_MODULE`
2. 点击 **新增节点** 按钮
3. 配置统计分析节点：
   - **节点编码**：`SCORE_STATISTICS`
   - **节点名称**：`成绩统计分析`
   - **节点类型**：`REPORT_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/score/statistics`
   - **组件路径**：`views/score/Statistics.vue`
   - **排序**：4
   - **图标**：`el-icon-data-line`
   - **是否在菜单显示**：是
4. 点击 **保存** 按钮

##### 4.5 成绩单导出节点
1. 确保页面顶部的 **模块选择器** 仍为 `SCORE_MODULE`
2. 点击 **新增节点** 按钮
3. 配置导出节点：
   - **节点编码**：`SCORE_EXPORT`
   - **节点名称**：`成绩单导出`
   - **节点类型**：`BATCH_EXPORT_PAGE`
   - **关联表**：`SCORE_TABLE`
   - **路由路径**：`/score/export`
   - **组件路径**：`views/score/Export.vue`
   - **排序**：5
   - **图标**：`el-icon-download`
   - **是否在菜单显示**：是
4. 点击 **保存** 按钮

### 第九步：配置业务规则

#### 1. 配置学生相关业务规则
1. 点击左侧菜单的 **业务规则管理**
2. 点击 **新增规则** 按钮
3. 添加学号唯一性规则：
   - 规则编码：`RULE_STU_CODE_UNIQUE`
   - 模块编码：`STUDENT_MODULE`
   - 规则类型：`VALIDATION_RULE`
   - 规则内容：
     ```json
     {
       "field": "student_code",
       "type": "unique",
       "message": "学号已存在"
     }
     ```
   - 描述：`确保学生学号的唯一性`
4. 点击 **保存** 按钮

#### 2. 配置课程相关业务规则
1. 点击 **新增规则** 按钮
2. 添加课程编码唯一性规则：
   - 规则编码：`RULE_COURSE_CODE_UNIQUE`
   - 模块编码：`COURSE_MODULE`
   - 规则类型：`VALIDATION_RULE`
   - 规则内容：
     ```json
     {
       "field": "course_code",
       "type": "unique",
       "message": "课程编码已存在"
     }
     ```
   - 描述：`确保课程编码的唯一性`
3. 点击 **保存** 按钮

#### 3. 配置成绩相关业务规则
1. 点击 **新增规则** 按钮
2. 添加成绩唯一性规则（同一个学生同一门课程只能有一条成绩记录）：
   - 规则编码：`RULE_SCORE_UNIQUE`
   - 模块编码：`SCORE_MODULE`
   - 规则类型：`VALIDATION_RULE`
   - 规则内容：
     ```json
     {
       "fields": ["student_id", "course_id"],
       "type": "unique_combo",
       "message": "该学生的该课程成绩已存在"
     }
     ```
   - 描述：`确保每个学生每门课程只有一条成绩记录`
3. 点击 **保存** 按钮

### 第十步：生成业务系统代码

现在我们已经完成了所有必要的配置，可以生成实际的业务代码了。业务系统是代码生成的重要维度，它决定了生成代码的组织方式和命名空间。

#### 1. 生成完整的代码包

1. 点击左侧菜单的 **代码生成**
2. 在 **选择业务系统** 下拉框中选择 `STUDENT_SYSTEM`
   - 业务系统决定了生成代码的业务范围和关联关系
   - 只有关联到该业务系统的模块和表才会显示在选择列表中
3. 在 **选择表** 下拉框中选择要生成代码的表（例如 `STUDENT_TABLE`）
   - 可以选择多个表同时生成代码
   - 表之间的关联关系会被自动识别和处理
4. 在 **包名** 输入框中输入 `com.student.management`
   - 包名是生成Java代码的基础包路径
   - 建议使用业务系统的英文名称作为包名前缀
5. 点击 **生成所有代码** 按钮
6. 生成完成后，系统将显示生成的所有代码文件，包括：
   - 数据库建表SQL：包含所有选中表的创建语句和关联关系
   - Java后端代码：Entity、Controller、Service、Mapper等完整的后端分层架构
   - Vue前端页面：列表页、表单页等前端组件
   - 路由配置：自动生成的前端路由和菜单配置
7. 点击 **下载代码包** 按钮，将生成的代码下载到本地

#### 2. 系统支持生成的代码类型

系统当前支持生成以下类型的代码文件：

| 代码类型 | 用途说明 |
|---------|----------|
| 数据库建表SQL | 生成MySQL数据库表创建语句 |
| Java实体类 | 生成与数据库表对应的Java实体类 |
| Java Mapper接口 | 生成MyBatis Mapper接口 |
| MyBatis Mapper XML | 生成MyBatis SQL映射配置文件 |
| Java Service层 | 生成业务逻辑层代码 |
| Java Controller层 | 生成RESTful API控制器 |
| Vue列表页面 | 生成前端数据列表展示页面 |
| Vue表单页面 | 生成前端数据新增/编辑表单页面 |
| Vue路由配置 | 生成前端路由配置 |

> **注意**：当前系统仅支持生成列表页和表单页的Vue模板，详情页、自定义页面等其他类型的页面需要手动实现。

#### 3. 业务系统在代码生成中的作用

- **代码组织**：业务系统作为代码的顶层组织单位，生成的代码会按照业务系统进行分类
- **关联关系**：自动处理同一业务系统内表之间的关联关系
- **命名空间**：影响生成代码的包名、类名和变量名
- **权限控制**：生成的代码包含基于业务系统的权限控制逻辑
- **配置管理**：生成业务系统级别的配置文件和初始化脚本

#### 3. 生成其他模块代码

重复上述步骤，为其他表生成代码：

| 表编码 | 包名 |
|--------|------|
| CLASS_TABLE | com.student.management |
| TEACHER_TABLE | com.student.management |
| COURSE_TABLE | com.student.management |
| SCORE_TABLE | com.student.management |

#### 4. 生成代码的使用要求

生成的代码不能直接使用，需要满足以下条件：

##### 4.1 后端必需依赖

需要在Maven pom.xml中添加以下依赖：

```xml
<!-- Spring Boot Web -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>

<!-- MyBatis -->
<dependency>
    <groupId>org.mybatis.spring.boot</groupId>
    <artifactId>mybatis-spring-boot-starter</artifactId>
    <version>2.3.1</version>
</dependency>

<!-- MySQL Driver -->
<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-java</artifactId>
    <version>8.0.33</version>
</dependency>

<!-- Lombok -->
<dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <version>1.18.34</version>
</dependency>
```

##### 4.2 后端通用类创建

需要创建以下三个通用类：

1. **Result.java** - 统一响应结果类
   ```java
   package ${packageName}.common;
   
   import lombok.Data;
   import java.io.Serializable;
   
   @Data
   public class Result<T> implements Serializable {
    private Integer code;

    /**
     * 响应消息
     */
    private String message;

    /**
     * 响应数据
     */
    private T data;

    public Result() {
    }

    public Result(Integer code, String message, T data) {
        this.code = code;
        this.message = message;
        this.data = data;
    }

    public static <T> Result<T> success() {
        return new Result<>(200, "操作成功", null);
    }

    public static <T> Result<T> success(T data) {
        return new Result<>(200, "操作成功", data);
    }

    public static <T> Result<T> error(String message) {
        return new Result<>(500, message, null);
    }
   }
   ```

2. **PageRequest.java** - 分页请求类
   ```java
   package ${packageName}.common;
   
   import lombok.Data;
   
   @Data
   public class PageRequest {
       private Integer current = 1;
       private Integer size = 10;
       
       public Integer getOffset() {
           return (current - 1) * size;
       }
   }
   ```

3. **PageResult.java** - 分页结果类
   ```java
   package ${packageName}.common;
   
   import lombok.Data;
   import java.util.List;
   
   @Data
   public class PageResult<T> {
       private Long total;
       private List<T> records;
   }
   ```

##### 4.3 前端依赖要求

需要安装以下npm依赖：
```bash
npm install vue@3 axios element-plus vue-router
```

##### 4.4 前端工具类

需要创建`src/utils/request.js`工具类：
```javascript
import axios from 'axios';

const service = axios.create({
  baseURL: 'http://localhost:8080/metadata-system/api/',
  timeout: 10000
});

// 请求拦截器
service.interceptors.request.use(
  config => {
    // 添加token等认证信息
    return config;
  },
  error => {
    return Promise.reject(error);
  }
);

// 响应拦截器
service.interceptors.response.use(
  response => {
    return response.data;
  },
  error => {
    return Promise.reject(error);
  }
);

export default service;
```

#### 5. 生成代码的目录结构

##### 5.1 后端代码目录结构

```
└── src
    └── main
        ├── java
        │   └── com
        │       └── student
        │           └── management
        │               ├── common       # 通用类目录
        │               │   ├── Result.java
        │               │   ├── PageRequest.java
        │               │   └── PageResult.java
        │               ├── controller    # 控制器目录
        │               │   ├── StudentController.java
        │               │   ├── ClassController.java
        │               │   ├── TeacherController.java
        │               │   ├── CourseController.java
        │               │   └── ScoreController.java
        │               ├── entity        # 实体类目录
        │               │   ├── Student.java
        │               │   ├── Class.java
        │               │   ├── Teacher.java
        │               │   ├── Course.java
        │               │   └── Score.java
        │               ├── mapper        # Mapper接口目录
        │               │   ├── StudentMapper.java
        │               │   ├── ClassMapper.java
        │               │   ├── TeacherMapper.java
        │               │   ├── CourseMapper.java
        │               │   └── ScoreMapper.java
        │               └── service       # 服务层目录
        │                   ├── StudentService.java
        │                   ├── ClassService.java
        │                   ├── TeacherService.java
        │                   ├── CourseService.java
        │                   └── ScoreService.java
        └── resources
            └── mapper                    # Mapper XML目录
                ├── StudentMapper.xml
                ├── ClassMapper.xml
                ├── TeacherMapper.xml
                ├── CourseMapper.xml
                └── ScoreMapper.xml
```

##### 5.2 前端代码目录结构

```
└── src
    ├── api                    # API方法目录
    │   ├── student.js
    │   ├── class.js
    │   ├── teacher.js
    │   ├── course.js
    │   └── score.js
    ├── components             # 组件目录
    ├── utils                  # 工具类目录
    │   └── request.js
    └── views                  # 页面视图目录
        ├── student
        │   ├── List.vue
        │   ├── Form.vue
        │   └── Detail.vue
        ├── class
        │   ├── List.vue
        │   ├── Form.vue
        │   └── Students.vue
        ├── teacher
        │   ├── List.vue
        │   └── Form.vue
        ├── course
        │   ├── List.vue
        │   ├── Form.vue
        │   └── Scores.vue
        └── score
            ├── List.vue
            ├── Form.vue
            ├── BatchInput.vue
            ├── Statistics.vue
            └── Export.vue
```

#### 6. 生成代码后的配置步骤

##### 6.1 配置概述

生成代码后，需要进行以下核心配置：

1. **数据库配置**：配置数据库连接信息，详细步骤请参考**第十一章 部署学生管理系统**
2. **MyBatis配置**：配置Mapper扫描路径，详细步骤请参考**第十一章 部署学生管理系统**
3. **前端API配置**：配置request.js的baseURL，详细步骤请参考**第十一章 部署学生管理系统**

##### 6.2 前端API方法创建

为每个模块创建API方法，例如`src/api/student.js`：

```javascript
import request from '@/utils/request';

export const StudentApi = {
  page: (params) => request.get('/api/student/page', { params }),
  list: (params) => request.get('/api/student/list', { params }),
  getById: (id) => request.get(`/api/student/${id}`),
  add: (data) => request.post('/api/student/add', data),
  update: (data) => request.post('/api/student/update', data),
  delete: (data) => request.post('/api/student/delete', data),
  batchDelete: (data) => request.post('/api/student/batchDelete', data)
};
```

##### 6.3 路由配置

将生成的`routes.js`集成到前端项目的路由配置中：

```javascript
import { createRouter, createWebHashHistory } from 'vue-router';
import studentRoutes from './student.routes';
import classRoutes from './class.routes';
import teacherRoutes from './teacher.routes';
import courseRoutes from './course.routes';
import scoreRoutes from './score.routes';

const routes = [
  {
    path: '/',
    redirect: '/student/list'
  },
  ...studentRoutes,
  ...classRoutes,
  ...teacherRoutes,
  ...courseRoutes,
  ...scoreRoutes
];

const router = createRouter({
  history: createWebHashHistory(),
  routes
});

export default router;
```

> **注意**：详细的部署配置步骤请参考**第十一章 部署学生管理系统**，包括数据库配置、MyBatis配置和前端API地址配置等。

#### 7. 生成代码的最佳实践

##### 7.1 分层设计原则

- **Controller层**：只负责接收请求和返回响应，不处理业务逻辑
- **Service层**：处理核心业务逻辑，调用Mapper层操作数据库
- **Mapper层**：只负责数据库操作，不包含业务逻辑
- **Entity层**：与数据库表一一对应，使用Lombok简化代码

##### 7.2 统一响应格式

- 所有API返回统一的Result格式
- 包含code（状态码）、message（提示信息）和data（数据）字段
- 成功返回200状态码，失败返回500状态码

##### 7.3 异常处理机制

- 实现全局异常处理器
- 捕获并处理业务异常和系统异常
- 返回友好的错误提示信息

##### 7.4 参数验证方法

- 在Controller层使用@Validated注解验证请求参数
- 为实体类添加javax.validation注解
- 自定义验证规则和错误提示

##### 7.5 日志记录建议

- 使用SLF4J+Logback进行日志记录
- 在关键操作点添加日志
- 日志级别使用合理（DEBUG、INFO、WARN、ERROR）

##### 7.6 代码规范要求

- 遵循Java编码规范
- 使用驼峰命名法
- 类、方法、变量添加注释
- 保持代码简洁，避免冗余

##### 7.7 代码复用策略

- 提取通用方法和工具类
- 使用继承和接口实现代码复用
- 避免重复代码

#### 8. 常见问题及解决方案

##### 8.1 生成的代码无法编译

**问题**：生成的代码无法通过Maven编译

**解决方案**：
- 检查是否添加了所有必需的依赖
- 检查包名是否正确配置
- 检查通用类是否已创建
- 检查Java版本是否兼容（要求JDK 17）

##### 8.2 关联查询数据显示为空

**问题**：列表页或详情页中关联数据显示为空

**解决方案**：
- 检查表关联关系配置是否正确
- 检查Mapper XML中的关联查询SQL是否正确
- 检查前端是否正确处理了关联数据

##### 8.3 节点创建后在菜单中不显示

**问题**：创建的功能节点在菜单中不显示

**解决方案**：
- 检查节点的"是否在菜单显示"配置是否为"是"
- 检查节点的排序设置是否合理
- 检查业务系统是否已关联该模块

##### 8.4 节点路由访问失败

**问题**：访问节点路由时出现404错误

**解决方案**：
- 检查路由路径配置是否正确
- 检查前端路由配置是否已集成生成的路由
- 检查组件路径是否正确

##### 8.5 节点关联表配置错误

**问题**：节点关联表配置错误，导致功能异常

**解决方案**：
- 修改节点的关联表配置
- 重新生成代码
- 检查前端组件中的表字段使用是否正确

#### 9. 生成代码的测试步骤

##### 9.1 后端API测试

1. **启动后端服务**：
   ```bash
   mvn spring-boot:run
   ```

2. **测试API接口**：
   - 使用Postman或curl测试各个API接口
   - 测试GET请求：`GET http://localhost:8080/api/student/list`
   - 测试POST请求：`POST http://localhost:8080/api/student/add`
   - 测试分页请求：`GET http://localhost:8080/api/student/page?current=1&size=10`
   - 测试批量删除请求：`POST http://localhost:8080/api/student/batchDelete`

##### 9.2 测试衔接说明

- **前端功能测试**：详细的前端功能测试步骤请参考**第十二章 系统功能测试**
- **完整功能测试案例**：请参考**第十二章 系统功能测试**中的测试内容
- **测试注意事项**：确保所有API接口返回正确的响应格式，前端功能正常运行

> **注意**：详细的功能测试内容请参考**第十二章 系统功能测试**，包括基础功能测试和关联功能测试。

### 第十一步：部署学生管理系统

#### 1. 数据库部署

1. 创建学生管理系统数据库：
   ```sql
   CREATE DATABASE IF NOT EXISTS `student_management` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   USE `student_management`;
   ```

2. 执行生成的SQL建表语句，为每个表创建相应的表结构

#### 2. 后端部署

1. 将生成的Java代码复制到您的Spring Boot项目中
2. 配置数据库连接信息（在 `application.yml` 或 `application.properties` 中）：
   ```properties
   spring.datasource.url=jdbc:mysql://localhost:3306/student_management?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
   spring.datasource.username=root
   spring.datasource.password=your_password
   ```

3. 配置MyBatis：
   ```properties
   mybatis.mapper-locations=classpath:mapper/*.xml
   mybatis.type-aliases-package=com.student.management
   ```

4. 配置业务系统（可选）：
   如果您的应用需要支持多业务系统，可以在配置文件中添加业务系统相关配置：
   ```properties
   # 业务系统配置
   business.system.default=STUDENT_SYSTEM
   # 业务系统数据源配置（如果需要多数据源）
   # spring.datasource.student.url=jdbc:mysql://localhost:3306/student_management
   ```

5. 启动后端服务：
   ```bash
   mvn spring-boot:run
   ```

#### 3. 前端部署

1. 将生成的Vue组件复制到您的Vue项目中
2. 配置Vue Router，将生成的路由配置添加到您的路由文件中
3. 配置API请求地址，确保前端可以正确访问后端服务
   - 在`src/utils/request.js`中修改baseURL：
     ```javascript
     const service = axios.create({
       baseURL: 'http://localhost:8080/metadata-system/api/',
       timeout: 10000
     })
     ```
4. 启动前端服务：
   ```bash
   # 在前端项目根目录执行
   npm run serve
   ```

5. 访问学生管理系统：
   - 前端地址：`http://localhost:8080/metadata-system/`
   - 学生列表页面：`http://localhost:8080/metadata-system/#/student/list`
   - 班级列表页面：`http://localhost:8080/metadata-system/#/class/list`
   - 课程列表页面：`http://localhost:8080/metadata-system/#/course/list`
   - 成绩列表页面：`http://localhost:8080/metadata-system/#/score/list`

> **注意**：当前系统使用的是Vue 3 + Vue Router 4，路由配置使用的是hash模式，因此URL中会包含`#`符号。

## 第十二步：系统功能测试

### 12.1 基础功能测试

1. **学生管理功能测试**：
   - 添加新学生，验证必填字段和格式验证
   - 编辑学生信息，验证更新功能
   - 删除学生，验证删除功能
   - 搜索和筛选学生，验证查询功能
   - 查看学生详情，验证关联信息显示

2. **班级管理功能测试**：
   - 添加新班级，验证班主任选择
   - 编辑班级信息，验证更新功能
   - 删除班级，验证删除功能
   - 查看班级学生列表，验证一对多关系

3. **课程管理功能测试**：
   - 添加新课程，验证课程信息
   - 编辑课程信息，验证更新功能
   - 删除课程，验证删除功能
   - 查看课程详情，验证授课教师信息

4. **成绩管理功能测试**：
   - 录入学生成绩，验证唯一性约束
   - 批量导入成绩，验证批量操作功能
   - 统计成绩数据，验证图表显示
   - 导出成绩单，验证文件格式

### 12.2 关联功能测试

1. **学生-班级关联测试**：
   - 在学生详情页查看班级信息
   - 在班级详情页查看学生列表
   - 修改学生所属班级，验证更新

2. **课程-成绩关联测试**：
   - 在课程详情页查看成绩列表
   - 按课程统计成绩分布

3. **学生-成绩关联测试**：
   - 在学生详情页查看成绩列表
   - 计算学生平均成绩

## 重要注意事项

### 1. 权限管理

- 生产环境中请修改默认密码（admin/123456）为强密码
- 建议配置适当的用户权限，限制不同角色的操作范围
- 定期备份数据库，防止数据丢失

### 2. 系统配置

- 确保数据库连接信息正确配置，特别是用户名和密码
- 生产环境中建议启用HTTPS
- 调整合适的JVM参数以优化性能
  ```bash
  java -Xms512m -Xmx1024m -jar target/student-management-1.0.0.jar
  ```

### 3. 数据安全

- 敏感数据（如学生身份证号）应进行加密存储
- 定期清理操作日志，避免数据库过大
- 实现数据备份和恢复策略

### 4. 性能优化

- 为数据库表的外键和常用查询字段添加索引
- 对于大数据量的查询，考虑使用分页和缓存
- 优化批量操作性能，避免频繁数据库交互

## 系统扩展建议

1. **集成Redis缓存**：提高系统响应速度
2. **添加考勤管理功能**：记录学生出勤情况
3. **实现家校互动平台**：家长可以查看学生成绩和表现
4. **添加宿舍管理模块**：管理学生住宿信息
5. **集成在线考试系统**：支持在线答题和自动评分
6. **添加数据分析功能**：基于大数据分析学生学习情况

通过上述扩展，可以使学生管理系统更加完善，满足更多复杂场景的需求。

## 总结

本指南详细介绍了如何使用元数据管理系统构建一个完整的学生管理系统，包含学生、班级、课程、成绩等核心功能模块。通过元数据配置和代码生成，您可以快速实现一个功能完善、结构清晰的学生管理系统。

系统的核心特点包括：
1. 完整的模块结构设计，包含学生、班级、课程、成绩等核心模块
2. 完善的表关联关系配置，支持一对多、多对一等关系
3. 丰富的功能节点配置，满足不同业务场景需求
4. 自动代码生成，快速实现前后端功能
5. 灵活的业务规则配置，支持数据验证和业务约束

通过本指南，您可以快速上手元数据管理系统，构建符合业务需求的学生管理系统，并根据实际需要进行功能扩展和定制化开发。

祝您使用愉快！