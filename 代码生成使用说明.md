# 代码生成使用说明

## 📋 概述

代码生成器可以生成完整的业务系统代码，包括：
- **SQL建表语句** - 可直接在数据库中执行
- **Java后端代码** - Entity、Controller、Service、Mapper、Mapper.xml
- **Vue前端代码** - List.vue（列表页，支持批量删除）、Form.vue（表单页）

**新功能**：生成的代码现已支持批量删除操作，包括：
- 后端批量删除接口（Controller、Service、Mapper）
- 前端批量删除功能（列表页复选框、批量删除按钮）

## ⚠️ 重要提示

**生成的代码不能直接使用，需要满足以下条件：**

### 1. 后端代码使用要求

#### 1.1 必需的依赖（Maven pom.xml）

```xml
<!-- Spring Boot Web -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>

<!-- MyBatis -->
<dependency>
    <groupId>org.mybatis.spring.boot</groupId>
    <artifactId>mybatis-spring-boot-starter</artifactId>
    <version>2.3.1</version>
</dependency>

<!-- MySQL Driver -->
<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-java</artifactId>
    <version>8.0.33</version>
</dependency>

<!-- Lombok -->
<dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <version>1.18.34</version>
</dependency>
```

#### 1.2 必需的通用类

生成的代码依赖以下通用类，**需要手动创建**或从本项目中复制：

**必须创建的类：**

1. **`Result.java`** - 统一响应结果类
   ```java
   package ${packageName}.common;
   
   import lombok.Data;
   import java.io.Serializable;
   
   @Data
   public class Result<T> implements Serializable {
       private Integer code;
       private String message;
       private T data;
       
       public static <T> Result<T> success() {
           return new Result<>(200, "操作成功", null);
       }
       
       public static <T> Result<T> success(T data) {
           return new Result<>(200, "操作成功", data);
       }
       
       public static <T> Result<T> error(String message) {
           return new Result<>(500, message, null);
       }
   }
   ```

2. **`PageRequest.java`** - 分页请求类
   ```java
   package ${packageName}.common;
   
   import lombok.Data;
   
   @Data
   public class PageRequest {
       private Integer current = 1;
       private Integer size = 10;
       
       public Integer getOffset() {
           return (current - 1) * size;
       }
   }
   ```

3. **`PageResult.java`** - 分页结果类
   ```java
   package ${packageName}.common;
   
   import lombok.Data;
   import java.util.List;
   
   @Data
   public class PageResult<T> {
       private Long total;
       private List<T> records;
       
       public PageResult(Long total, List<T> records) {
           this.total = total;
           this.records = records;
       }
   }
   ```

**获取方式：**
- 从本项目复制：`src/main/java/com/metadata/common/` 目录下的三个文件
- 修改包名为您项目的包名

#### 1.3 必需的配置

**application.yml 配置：**

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/your_database?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: your_username
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: ${packageName}.entity
```

**MyBatis 配置类（可选）：**

```java
@Configuration
@MapperScan("${packageName}.mapper")
public class MyBatisConfig {
}
```

#### 1.4 代码放置位置

生成的代码需要放在正确的包路径下：

```
src/main/java/${packageName}/
├── common/          # 通用类（Result、PageRequest、PageResult）
├── entity/          # 实体类（生成的 Entity.java）
├── controller/      # 控制器（生成的 Controller.java）
├── service/         # 服务类（生成的 Service.java）
└── mapper/          # Mapper接口（生成的 Mapper.java）

src/main/resources/mapper/
└── ${className}Mapper.xml  # Mapper XML文件
```

### 2. 前端代码使用要求

#### 2.1 必需的依赖（package.json）

```json
{
  "dependencies": {
    "vue": "^3.3.4",
    "vue-router": "^4.2.5",
    "axios": "^1.6.2",
    "element-plus": "^2.4.4"
  }
}
```

#### 2.2 必需的 API 工具类

生成的 Vue 代码需要 API 调用工具，**需要创建**：

**`src/api/index.js`** - API 接口定义（示例）

```javascript
import request from '../utils/request'

// 根据生成的代码，创建对应的 API 对象
export const ${componentName}Api = {
  // 分页查询
  page: (params) => request.get('/api/${entityName}/page', { params }),
  // 列表查询
  list: (params) => request.get('/api/${entityName}/list', { params }),
  // 根据ID查询
  getById: (id) => request.get(`/api/${entityName}/${id}`),
  // 新增
  add: (data) => request.post('/api/${entityName}/add', data),
  // 更新
  update: (data) => request.post('/api/${entityName}/update', data),
  // 删除
  delete: (data) => request.post('/api/${entityName}/delete', data),
  // 批量删除
  batchDelete: (data) => request.post('/api/${entityName}/batchDelete', data)
}
```

**注意**：生成的Vue代码使用 `${componentName}Api` 对象来调用API，所以需要创建API对象而不是单独的函数。

**`src/utils/request.js`** - Axios 请求封装（示例）

```javascript
import axios from 'axios'

const request = axios.create({
  baseURL: 'http://localhost:8080',
  timeout: 10000
})

// 请求拦截器
request.interceptors.request.use(
  config => {
    // 可以在这里添加 token 等
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code === 200) {
      return res
    } else {
      return Promise.reject(new Error(res.message || '请求失败'))
    }
  },
  error => {
    return Promise.reject(error)
  }
)

export default request
```

#### 2.3 路由配置

需要在 `router/index.js` 中添加路由：

```javascript
{
  path: '/${entityName}',
  name: '${ClassName}List',
  component: () => import('@/views/${ClassName}List.vue')
}
```

### 3. 使用步骤

#### 步骤 1：执行 SQL 建表

1. 复制生成的 `create_table.sql` 内容
2. 在数据库中执行 SQL 语句
3. 确认表创建成功

#### 步骤 2：配置后端项目

1. **创建通用类**：
   - 复制 `Result.java`、`PageRequest.java`、`PageResult.java` 到 `src/main/java/${packageName}/common/`
   - 修改包名为您的包名

2. **放置生成的代码**：
   - Entity → `src/main/java/${packageName}/entity/`
   - Controller → `src/main/java/${packageName}/controller/`
   - Service → `src/main/java/${packageName}/service/`
   - Mapper → `src/main/java/${packageName}/mapper/`
   - Mapper.xml → `src/main/resources/mapper/`

3. **配置 MyBatis**：
   - 确保 `application.yml` 中配置了 MyBatis mapper 路径
   - 确保 `@MapperScan` 扫描了正确的包

4. **启动项目测试**：
   - 启动 Spring Boot 应用
   - 测试 API 接口是否正常

#### 步骤 3：配置前端项目

1. **创建 API 文件**：
   - 在 `src/api/` 目录创建对应的 API 方法
   - 确保 API 路径与后端 Controller 路径一致

2. **放置生成的 Vue 文件**：
   - List.vue → `src/views/${ClassName}List.vue`
   - Form.vue → `src/views/${ClassName}Form.vue`（如果需要独立表单页）

3. **配置路由**：
   - 在路由配置中添加页面路由

4. **测试前端页面**：
   - 启动前端项目
   - 访问列表页，测试增删改查功能

### 4. 常见问题

#### Q1: 编译错误 - 找不到 Result 类
**A:** 需要创建 `Result.java`、`PageRequest.java`、`PageResult.java` 三个通用类

#### Q2: 运行时错误 - Mapper 接口找不到
**A:** 检查 `@MapperScan` 是否扫描了正确的包路径

#### Q3: 前端 API 调用失败
**A:** 
- 检查 `request.js` 中的 `baseURL` 是否正确
- 检查后端接口路径是否与前端 API 路径一致
- 检查跨域配置（CORS）

#### Q4: 数据库连接失败
**A:** 检查 `application.yml` 中的数据库配置是否正确

#### Q5: 生成的代码包名不对
**A:** 在代码生成时输入正确的包名，或生成后手动替换包名

### 5. 快速检查清单

使用生成的代码前，请确认：

**后端：**
- [ ] 已添加必需的 Maven 依赖
- [ ] 已创建 `Result`、`PageRequest`、`PageResult` 三个通用类
- [ ] 已配置 `application.yml` 数据库连接
- [ ] 已配置 MyBatis mapper 扫描路径
- [ ] 已执行 SQL 建表语句
- [ ] 生成的代码已放在正确的包路径下

**前端：**
- [ ] 已安装必需的 npm 依赖（vue、axios、element-plus）
- [ ] 已创建 `request.js` 工具类
- [ ] 已创建对应的 API 方法
- [ ] 已配置路由
- [ ] 已检查 API 路径与后端一致

### 6. 示例：完整使用流程

假设生成的是学生管理模块（STUDENT_TABLE）：

1. **执行 SQL**：
   ```sql
   CREATE TABLE IF NOT EXISTS `student` (
     `id` bigint NOT NULL AUTO_INCREMENT,
     `student_code` varchar(50) NOT NULL,
     `student_name` varchar(100) NOT NULL,
     ...
     PRIMARY KEY (`id`)
   );
   ```

2. **创建通用类**（包名：`com.example`）：
   - `com.example.common.Result`
   - `com.example.common.PageRequest`
   - `com.example.common.PageResult`

3. **放置生成的代码**：
   - `com.example.entity.Student`
   - `com.example.controller.StudentController`
   - `com.example.service.StudentService`
   - `com.example.mapper.StudentMapper`
   - `mapper/StudentMapper.xml`

4. **创建前端 API**：
   ```javascript
   export const StudentApi = {
     page: (params) => request.get('/api/student/page', { params }),
     list: (params) => request.get('/api/student/list', { params }),
     getById: (id) => request.get(`/api/student/${id}`),
     add: (data) => request.post('/api/student/add', data),
     update: (data) => request.post('/api/student/update', data),
     delete: (data) => request.post('/api/student/delete', data),
     batchDelete: (data) => request.post('/api/student/batchDelete', data)
   }
   ```

5. **测试**：
   - 启动后端，访问 `http://localhost:8080/api/student/list`
   - 启动前端，访问列表页测试功能

## 📝 总结

生成的代码**结构完整**，但需要：
1. ✅ 满足依赖要求（Maven/npm）
2. ✅ 创建必需的通用类（Result、PageRequest、PageResult）
3. ✅ 配置数据库和 MyBatis
4. ✅ 创建前端 API 工具类
5. ✅ 正确放置代码到对应目录

完成以上步骤后，生成的代码就可以正常使用了！

