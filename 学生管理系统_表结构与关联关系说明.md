# 学生管理系统 - 表结构与关联关系详细说明

## 一、整体数据库结构概述

学生管理系统采用关系型数据库设计，包含以下五个核心表：
1. 班级信息表（CLASS_TABLE）
2. 教师信息表（TEACHER_TABLE）
3. 学生信息表（STUDENT_TABLE）
4. 课程信息表（COURSE_TABLE）
5. 成绩信息表（SCORE_TABLE）

这些表之间通过外键建立关联，形成一个完整的数据模型，支持学生、班级、课程、教师和成绩之间的复杂查询和数据操作。

## 二、表结构详细设计

### 1. 班级信息表（CLASS_TABLE）

| 字段名 | 字段类型 | 长度 | 是否为空 | 描述 | 约束 |
|-------|---------|-----|---------|------|------|
| CLASS_ID | BIGINT | 20 | 否 | 班级ID | 主键，自增 |
| CLASS_CODE | VARCHAR | 50 | 否 | 班级编号 | 唯一索引 |
| CLASS_NAME | VARCHAR | 100 | 否 | 班级名称 | |
| GRADE | VARCHAR | 50 | 是 | 年级 | |
| HEAD_TEACHER_ID | BIGINT | 20 | 是 | 班主任ID | 外键，引用TEACHER_TABLE.TEACHER_ID |
| STUDENT_COUNT | INT | 11 | 是 | 学生人数 | 默认为0 |
| CREATE_TIME | DATETIME | | 是 | 创建时间 | 自动生成 |
| UPDATE_TIME | DATETIME | | 是 | 更新时间 | 自动生成 |

**SQL建表语句：**
```sql
CREATE TABLE IF NOT EXISTS `CLASS_TABLE` (
  `CLASS_ID` BIGINT NOT NULL AUTO_INCREMENT COMMENT '班级ID',
  `CLASS_CODE` VARCHAR(50) NOT NULL COMMENT '班级编号',
  `CLASS_NAME` VARCHAR(100) NOT NULL COMMENT '班级名称',
  `GRADE` VARCHAR(50) DEFAULT NULL COMMENT '年级',
  `HEAD_TEACHER_ID` BIGINT DEFAULT NULL COMMENT '班主任ID',
  `STUDENT_COUNT` INT DEFAULT 0 COMMENT '学生人数',
  `CREATE_TIME` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `UPDATE_TIME` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`CLASS_ID`),
  UNIQUE KEY `UK_CLASS_CODE` (`CLASS_CODE`),
  KEY `FK_CLASS_HEAD_TEACHER` (`HEAD_TEACHER_ID`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='班级信息表';
```

### 2. 教师信息表（TEACHER_TABLE）

| 字段名 | 字段类型 | 长度 | 是否为空 | 描述 | 约束 |
|-------|---------|-----|---------|------|------|
| TEACHER_ID | BIGINT | 20 | 否 | 教师ID | 主键，自增 |
| TEACHER_CODE | VARCHAR | 50 | 否 | 教师编号 | 唯一索引 |
| TEACHER_NAME | VARCHAR | 100 | 否 | 教师姓名 | |
| GENDER | VARCHAR | 10 | 是 | 性别 | |
| TITLE | VARCHAR | 50 | 是 | 职称 | |
| PHONE | VARCHAR | 20 | 是 | 手机号 | |
| EMAIL | VARCHAR | 100 | 是 | 邮箱 | |
| CREATE_TIME | DATETIME | | 是 | 创建时间 | 自动生成 |
| UPDATE_TIME | DATETIME | | 是 | 更新时间 | 自动生成 |

**SQL建表语句：**
```sql
CREATE TABLE IF NOT EXISTS `TEACHER_TABLE` (
  `TEACHER_ID` BIGINT NOT NULL AUTO_INCREMENT COMMENT '教师ID',
  `TEACHER_CODE` VARCHAR(50) NOT NULL COMMENT '教师编号',
  `TEACHER_NAME` VARCHAR(100) NOT NULL COMMENT '教师姓名',
  `GENDER` VARCHAR(10) DEFAULT NULL COMMENT '性别',
  `TITLE` VARCHAR(50) DEFAULT NULL COMMENT '职称',
  `PHONE` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `EMAIL` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
  `CREATE_TIME` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `UPDATE_TIME` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`TEACHER_ID`),
  UNIQUE KEY `UK_TEACHER_CODE` (`TEACHER_CODE`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='教师信息表';
```

### 3. 学生信息表（STUDENT_TABLE）

| 字段名 | 字段类型 | 长度 | 是否为空 | 描述 | 约束 |
|-------|---------|-----|---------|------|------|
| STU_ID | BIGINT | 20 | 否 | 学生ID | 主键，自增 |
| STUDENT_CODE | VARCHAR | 50 | 否 | 学号 | 唯一索引 |
| STUDENT_NAME | VARCHAR | 100 | 否 | 姓名 | |
| GENDER | VARCHAR | 10 | 是 | 性别 | |
| AGE | INT | 11 | 是 | 年龄 | |
| PHONE | VARCHAR | 20 | 是 | 手机号 | |
| EMAIL | VARCHAR | 100 | 是 | 邮箱 | |
| CLASS_ID | BIGINT | 20 | 是 | 班级ID | 外键，引用CLASS_TABLE.CLASS_ID |
| ENTRY_DATE | DATE | | 是 | 入学日期 | |
| ADDRESS | VARCHAR | 200 | 是 | 地址 | |
| STATUS | VARCHAR | 20 | 是 | 状态 | |
| CREATE_TIME | DATETIME | | 是 | 创建时间 | 自动生成 |
| UPDATE_TIME | DATETIME | | 是 | 更新时间 | 自动生成 |

**SQL建表语句：**
```sql
CREATE TABLE IF NOT EXISTS `STUDENT_TABLE` (
  `STU_ID` BIGINT NOT NULL AUTO_INCREMENT COMMENT '学生ID',
  `STUDENT_CODE` VARCHAR(50) NOT NULL COMMENT '学号',
  `STUDENT_NAME` VARCHAR(100) NOT NULL COMMENT '姓名',
  `GENDER` VARCHAR(10) DEFAULT NULL COMMENT '性别',
  `AGE` INT DEFAULT NULL COMMENT '年龄',
  `PHONE` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `EMAIL` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
  `CLASS_ID` BIGINT DEFAULT NULL COMMENT '班级ID',
  `ENTRY_DATE` DATE DEFAULT NULL COMMENT '入学日期',
  `ADDRESS` VARCHAR(200) DEFAULT NULL COMMENT '地址',
  `STATUS` VARCHAR(20) DEFAULT NULL COMMENT '状态',
  `CREATE_TIME` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `UPDATE_TIME` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`STU_ID`),
  UNIQUE KEY `UK_STUDENT_CODE` (`STUDENT_CODE`),
  KEY `FK_STUDENT_CLASS` (`CLASS_ID`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='学生信息表';
```

### 4. 课程信息表（COURSE_TABLE）

| 字段名 | 字段类型 | 长度 | 是否为空 | 描述 | 约束 |
|-------|---------|-----|---------|------|------|
| COURSE_ID | BIGINT | 20 | 否 | 课程ID | 主键，自增 |
| COURSE_CODE | VARCHAR | 50 | 否 | 课程编号 | 唯一索引 |
| COURSE_NAME | VARCHAR | 100 | 否 | 课程名称 | |
| COURSE_TYPE | VARCHAR | 50 | 是 | 课程类型 | |
| CREDITS | DECIMAL | 4,1 | 是 | 学分 | |
| HOURS | INT | 11 | 是 | 课时 | |
| DESCRIPTION | TEXT | | 是 | 课程描述 | |
| TEACHER_ID | BIGINT | 20 | 是 | 授课教师ID | 外键，引用TEACHER_TABLE.TEACHER_ID |
| CREATE_TIME | DATETIME | | 是 | 创建时间 | 自动生成 |
| UPDATE_TIME | DATETIME | | 是 | 更新时间 | 自动生成 |

**SQL建表语句：**
```sql
CREATE TABLE IF NOT EXISTS `COURSE_TABLE` (
  `COURSE_ID` BIGINT NOT NULL AUTO_INCREMENT COMMENT '课程ID',
  `COURSE_CODE` VARCHAR(50) NOT NULL COMMENT '课程编号',
  `COURSE_NAME` VARCHAR(100) NOT NULL COMMENT '课程名称',
  `COURSE_TYPE` VARCHAR(50) DEFAULT NULL COMMENT '课程类型',
  `CREDITS` DECIMAL(4,1) DEFAULT NULL COMMENT '学分',
  `HOURS` INT DEFAULT NULL COMMENT '课时',
  `DESCRIPTION` TEXT COMMENT '课程描述',
  `TEACHER_ID` BIGINT DEFAULT NULL COMMENT '授课教师ID',
  `CREATE_TIME` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `UPDATE_TIME` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`COURSE_ID`),
  UNIQUE KEY `UK_COURSE_CODE` (`COURSE_CODE`),
  KEY `FK_COURSE_TEACHER` (`TEACHER_ID`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='课程信息表';
```

### 5. 成绩信息表（SCORE_TABLE）

| 字段名 | 字段类型 | 长度 | 是否为空 | 描述 | 约束 |
|-------|---------|-----|---------|------|------|
| SCORE_ID | BIGINT | 20 | 否 | 成绩ID | 主键，自增 |
| STUDENT_ID | BIGINT | 20 | 否 | 学生ID | 外键，引用STUDENT_TABLE.STU_ID |
| COURSE_ID | BIGINT | 20 | 否 | 课程ID | 外键，引用COURSE_TABLE.COURSE_ID |
| SCORE | DECIMAL | 5,2 | 是 | 分数 | |
| EXAM_DATE | DATE | | 是 | 考试日期 | |
| SEMESTER | VARCHAR | 50 | 是 | 学期 | |
| REMARK | TEXT | | 是 | 备注 | |
| CREATE_TIME | DATETIME | | 是 | 创建时间 | 自动生成 |
| UPDATE_TIME | DATETIME | | 是 | 更新时间 | 自动生成 |

**SQL建表语句：**
```sql
CREATE TABLE IF NOT EXISTS `SCORE_TABLE` (
  `SCORE_ID` BIGINT NOT NULL AUTO_INCREMENT COMMENT '成绩ID',
  `STUDENT_ID` BIGINT NOT NULL COMMENT '学生ID',
  `COURSE_ID` BIGINT NOT NULL COMMENT '课程ID',
  `SCORE` DECIMAL(5,2) DEFAULT NULL COMMENT '分数',
  `EXAM_DATE` DATE DEFAULT NULL COMMENT '考试日期',
  `SEMESTER` VARCHAR(50) DEFAULT NULL COMMENT '学期',
  `REMARK` TEXT COMMENT '备注',
  `CREATE_TIME` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `UPDATE_TIME` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`SCORE_ID`),
  UNIQUE KEY `UK_STUDENT_COURSE` (`STUDENT_ID`,`COURSE_ID`),
  KEY `FK_SCORE_STUDENT` (`STUDENT_ID`),
  KEY `FK_SCORE_COURSE` (`COURSE_ID`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='成绩信息表';
```

## 三、表关联关系详解

### 1. 关联关系概述

学生管理系统包含以下几种关键关联关系：
1. 教师和班级：一对多关系（一个教师可以担任多个班级的班主任）
2. 班级和学生：一对多关系（一个班级包含多个学生）
3. 学生和班级：多对一关系（多个学生属于一个班级）
4. 教师和课程：一对多关系（一个教师可以教授多门课程）
5. 课程和成绩：一对多关系（一门课程有多个学生成绩）
6. 学生和成绩：一对多关系（一个学生有多门课程成绩）
7. 学生和课程：多对多关系（通过成绩表SCORE_TABLE实现）

### 2. 关联关系图

```
+-----------------+       +----------------+       +-----------------+
|   TEACHER_TABLE |       |   CLASS_TABLE  |       | STUDENT_TABLE   |
+-----------------+       +----------------+       +-----------------+
| TEACHER_ID (PK) |<----->| HEAD_TEACHER_ID|<----->| CLASS_ID (FK)   |
| TEACHER_CODE    |       | CLASS_ID (PK)  |       | STU_ID (PK)     |
| TEACHER_NAME    |       | CLASS_CODE     |       | STUDENT_CODE    |
| ...             |       | CLASS_NAME     |       | STUDENT_NAME    |
+-----------------+       +----------------+       +-----------------+
        ^                         ^                       ^
        |                         |                       |
        |                         |                       |
        v                         v                       v
+-----------------+       +----------------+       +-----------------+
|   COURSE_TABLE  |       |    SCORE_TABLE |       |    SCORE_TABLE  |
+-----------------+       +----------------+       +-----------------+
| COURSE_ID (PK)  |<----->| COURSE_ID (FK) |       | STUDENT_ID (FK) |
| TEACHER_ID (FK) |       | STUDENT_ID (FK)|       | SCORE_ID (PK)   |
| COURSE_CODE     |       | SCORE_ID (PK)  |       | SCORE           |
| COURSE_NAME     |       | SCORE          |       | ...             |
| ...             |       | ...            |       +-----------------+
+-----------------+       +----------------+
```

### 3. 外键约束配置

为了确保数据完整性，需要在数据库层面添加外键约束：

```sql
-- 教师与班级的外键约束
ALTER TABLE `CLASS_TABLE`
ADD CONSTRAINT `FK_CLASS_HEAD_TEACHER` 
FOREIGN KEY (`HEAD_TEACHER_ID`) 
REFERENCES `TEACHER_TABLE` (`TEACHER_ID`) 
ON DELETE SET NULL ON UPDATE CASCADE;

-- 学生与班级的外键约束
ALTER TABLE `STUDENT_TABLE`
ADD CONSTRAINT `FK_STUDENT_CLASS` 
FOREIGN KEY (`CLASS_ID`) 
REFERENCES `CLASS_TABLE` (`CLASS_ID`) 
ON DELETE SET NULL ON UPDATE CASCADE;

-- 课程与教师的外键约束
ALTER TABLE `COURSE_TABLE`
ADD CONSTRAINT `FK_COURSE_TEACHER` 
FOREIGN KEY (`TEACHER_ID`) 
REFERENCES `TEACHER_TABLE` (`TEACHER_ID`) 
ON DELETE SET NULL ON UPDATE CASCADE;

-- 成绩与学生的外键约束
ALTER TABLE `SCORE_TABLE`
ADD CONSTRAINT `FK_SCORE_STUDENT` 
FOREIGN KEY (`STUDENT_ID`) 
REFERENCES `STUDENT_TABLE` (`STU_ID`) 
ON DELETE CASCADE ON UPDATE CASCADE;

-- 成绩与课程的外键约束
ALTER TABLE `SCORE_TABLE`
ADD CONSTRAINT `FK_SCORE_COURSE` 
FOREIGN KEY (`COURSE_ID`) 
REFERENCES `COURSE_TABLE` (`COURSE_ID`) 
ON DELETE CASCADE ON UPDATE CASCADE;
```

## 四、字段配置与验证规则

### 1. 字段校验规则详解

以下是每个表的字段校验规则的详细说明：

#### 班级表（CLASS_TABLE）

| 字段编码 | 字段名称 | 校验规则 | 说明 |
|---------|---------|---------|------|
| CLASS_CODE | 班级编号 | `{"pattern": "^\\w{4,20}$", "message": "班级编号只能包含字母数字下划线，长度4-20位"}` | 确保班级编号格式正确 |
| STUDENT_COUNT | 学生人数 | `{"min": 0, "message": "学生人数不能为负数"}` | 确保学生人数为非负数 |

#### 教师表（TEACHER_TABLE）

| 字段编码 | 字段名称 | 校验规则 | 说明 |
|---------|---------|---------|------|
| TEACHER_CODE | 教师编号 | `{"pattern": "^\\w{4,20}$", "message": "教师编号只能包含字母数字下划线，长度4-20位"}` | 确保教师编号格式正确 |
| TEACHER_PHONE | 手机号 | `{"pattern": "^1[3-9]\\d{9}$", "message": "请输入正确的手机号"}` | 确保手机号格式正确 |
| TEACHER_EMAIL | 邮箱 | `{"pattern": "^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$", "message": "请输入正确的邮箱地址"}` | 确保邮箱格式正确 |

#### 学生表（STUDENT_TABLE）

| 字段编码 | 字段名称 | 校验规则 | 说明 |
|---------|---------|---------|------|
| STU_CODE | 学号 | `{"pattern": "^\\d{10}$", "message": "学号必须为10位数字"}` | 确保学号为10位数字 |
| STU_AGE | 年龄 | `{"min": 1, "max": 100, "message": "年龄必须在1-100之间"}` | 确保年龄在合理范围内 |
| STU_PHONE | 手机号 | `{"pattern": "^1[3-9]\\d{9}$", "message": "请输入正确的手机号"}` | 确保手机号格式正确 |
| STU_EMAIL | 邮箱 | `{"pattern": "^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$", "message": "请输入正确的邮箱地址"}` | 确保邮箱格式正确 |

#### 课程表（COURSE_TABLE）

| 字段编码 | 字段名称 | 校验规则 | 说明 |
|---------|---------|---------|------|
| COURSE_CODE | 课程编号 | `{"pattern": "^\\w{4,20}$", "message": "课程编号只能包含字母数字下划线，长度4-20位"}` | 确保课程编号格式正确 |
| CREDITS | 学分 | `{"min": 0, "max": 10, "message": "学分必须在0-10之间"}` | 确保学分为合理值 |
| HOURS | 课时 | `{"min": 0, "message": "课时不能为负数"}` | 确保课时为非负数 |

#### 成绩表（SCORE_TABLE）

| 字段编码 | 字段名称 | 校验规则 | 说明 |
|---------|---------|---------|------|
| SCORE | 分数 | `{"min": 0, "max": 100, "message": "分数必须在0-100之间"}` | 确保分数在有效范围内 |

### 2. 表单组件映射

以下是字段类型与前端表单组件的映射关系：

| 字段类型 | 表单组件 | 适用字段示例 |
|---------|---------|------------|
| VARCHAR | input | 学号、姓名、班级编号等 |
| INT | number | 年龄、课时、学生人数等 |
| DECIMAL | number | 学分、分数等 |
| DATE | datepicker | 入学日期、考试日期等 |
| DATETIME | datetimepicker | 创建时间、更新时间等 |
| TEXT | textarea | 课程描述、备注等 |
| BIGINT (外键) | select | 班级ID、教师ID、学生ID、课程ID等 |
| VARCHAR (枚举) | select | 性别、课程类型、学生状态等 |

## 五、业务规则配置

### 1. 数据唯一性规则

#### 学生模块唯一性规则

```json
{
  "field": "student_code",
  "type": "unique",
  "message": "学号已存在"
}
```

#### 课程模块唯一性规则

```json
{
  "field": "course_code",
  "type": "unique",
  "message": "课程编码已存在"
}
```

#### 成绩模块唯一性规则

```json
{
  "fields": ["student_id", "course_id"],
  "type": "unique_combo",
  "message": "该学生的该课程成绩已存在"
}
```

### 2. 级联操作规则

在表关联关系中，设置了以下级联操作规则：

1. **教师删除时**：
   - 班级表中的HEAD_TEACHER_ID设置为NULL（不删除班级）
   - 课程表中的TEACHER_ID设置为NULL（不删除课程）

2. **班级删除时**：
   - 学生表中的CLASS_ID设置为NULL（不删除学生）

3. **学生删除时**：
   - 成绩表中该学生的所有成绩记录会被级联删除

4. **课程删除时**：
   - 成绩表中该课程的所有成绩记录会被级联删除

5. **ID更新时**：
   - 所有关联表中的外键会被级联更新

这些级联规则确保了数据的完整性，同时避免了因误操作导致的数据丢失。

## 六、索引设计

为了提高查询性能，系统在以下字段上建立了索引：

### 1. 主键索引

每个表都有一个自增的主键，系统会自动为其创建主键索引。

### 2. 唯一索引

- `CLASS_TABLE`: `UK_CLASS_CODE` (CLASS_CODE)
- `TEACHER_TABLE`: `UK_TEACHER_CODE` (TEACHER_CODE)
- `STUDENT_TABLE`: `UK_STUDENT_CODE` (STUDENT_CODE)
- `COURSE_TABLE`: `UK_COURSE_CODE` (COURSE_CODE)
- `SCORE_TABLE`: `UK_STUDENT_COURSE` (STUDENT_ID, COURSE_ID)

### 3. 普通索引

- `CLASS_TABLE`: `FK_CLASS_HEAD_TEACHER` (HEAD_TEACHER_ID)
- `STUDENT_TABLE`: `FK_STUDENT_CLASS` (CLASS_ID)
- `COURSE_TABLE`: `FK_COURSE_TEACHER` (TEACHER_ID)
- `SCORE_TABLE`: `FK_SCORE_STUDENT` (STUDENT_ID)
- `SCORE_TABLE`: `FK_SCORE_COURSE` (COURSE_ID)

这些索引设计能够显著提高多表联查和条件查询的性能，特别是在数据量较大的情况下。

## 七、表关系使用示例

### 1. 查询一个班级的所有学生

```sql
SELECT s.*, c.CLASS_NAME 
FROM STUDENT_TABLE s
LEFT JOIN CLASS_TABLE c ON s.CLASS_ID = c.CLASS_ID
WHERE s.CLASS_ID = #{classId};
```

### 2. 查询一个学生的所有成绩

```sql
SELECT sc.SCORE_ID, sc.SCORE, sc.EXAM_DATE, sc.SEMESTER,
       c.COURSE_NAME, c.COURSE_CODE, c.CREDITS,
       t.TEACHER_NAME, t.TEACHER_CODE
FROM SCORE_TABLE sc
LEFT JOIN COURSE_TABLE c ON sc.COURSE_ID = c.COURSE_ID
LEFT JOIN TEACHER_TABLE t ON c.TEACHER_ID = t.TEACHER_ID
WHERE sc.STUDENT_ID = #{studentId};
```

### 3. 查询一门课程的所有学生成绩

```sql
SELECT sc.SCORE_ID, sc.SCORE, sc.EXAM_DATE, sc.SEMESTER,
       s.STUDENT_NAME, s.STUDENT_CODE, s.GENDER, s.AGE,
       c.CLASS_NAME
FROM SCORE_TABLE sc
LEFT JOIN STUDENT_TABLE s ON sc.STUDENT_ID = s.STU_ID
LEFT JOIN CLASS_TABLE c ON s.CLASS_ID = c.CLASS_ID
WHERE sc.COURSE_ID = #{courseId};
```

### 4. 计算每个班级的平均成绩

```sql
SELECT c.CLASS_NAME, AVG(sc.SCORE) as avg_score,
       COUNT(DISTINCT sc.STUDENT_ID) as student_count,
       COUNT(sc.SCORE_ID) as score_count
FROM CLASS_TABLE c
LEFT JOIN STUDENT_TABLE s ON c.CLASS_ID = s.CLASS_ID
LEFT JOIN SCORE_TABLE sc ON s.STU_ID = sc.STUDENT_ID
GROUP BY c.CLASS_ID, c.CLASS_NAME;
```

## 八、数据维护与优化建议

### 1. 定期数据备份

建议定期备份学生管理系统的数据库，以防止数据丢失。可以使用MySQL的`mysqldump`工具或其他数据库备份工具。

### 2. 索引维护

随着数据量的增长，需要定期检查和优化索引，确保查询性能。可以使用MySQL的`EXPLAIN`语句分析查询执行计划，找出性能瓶颈。

### 3. 数据归档

对于历史数据，可以考虑进行归档处理，将不常用的历史数据移至归档表，以提高当前数据库的查询性能。

### 4. 性能监控

建议设置数据库性能监控，定期查看慢查询日志，优化查询语句。对于频繁执行的复杂查询，可以考虑使用物化视图或缓存技术来提高性能。

### 5. 安全建议

- 定期更新数据库密码
- 限制数据库访问权限，只允许应用程序使用的账号访问数据库
- 对敏感数据（如学生个人信息）进行加密存储
- 实施SQL注入防护措施，使用参数化查询

通过以上设计和建议，学生管理系统的数据库将具有良好的结构、性能和安全性，能够满足日常教学管理的需求。