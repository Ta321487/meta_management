# 学生管理系统 - 部署与测试指南

## 一、部署准备工作

### 1. 系统环境要求

#### 1.1 硬件要求
- **CPU**: 至少2核
- **内存**: 至少4GB
- **磁盘空间**: 至少50GB可用空间
- **网络**: 稳定的网络连接

#### 1.2 软件要求
- **操作系统**: Windows Server 2016/2019/2022 或 Linux (CentOS 7/8, Ubuntu 18.04/20.04)
- **JDK**: JDK 11 或 JDK 1.8 (推荐 JDK 11)
- **Maven**: Maven 3.6.0+ (用于构建后端项目)
- **Node.js**: Node.js 14.x 或 16.x (用于构建前端项目)
- **数据库**: MySQL 5.7+ 或 PostgreSQL 10+ (推荐 MySQL 8.0)
- **Web服务器**: Nginx 1.16+ (用于部署前端项目)

### 2. 部署前的检查事项

- 确保JDK已正确安装并配置环境变量
- 确保Maven已正确安装并配置环境变量
- 确保Node.js和npm已正确安装
- 确保数据库服务已启动并可访问
- 确保端口8080（后端）和80（前端）未被占用
- 准备好部署所需的数据库脚本

## 二、数据库部署

### 1. MySQL数据库部署

#### 1.1 创建数据库和用户

```sql
-- 创建数据库
CREATE DATABASE student_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户并授权
CREATE USER 'student'@'%' IDENTIFIED BY 'Student@123';
GRANT ALL PRIVILEGES ON student_management.* TO 'student'@'%';
FLUSH PRIVILEGES;
```

#### 1.2 导入数据库脚本

将生成的SQL脚本（`student_management.sql`）导入到刚创建的数据库中：

```bash
# 方法一：使用mysql命令行导入
mysql -u student -p student_management < student_management.sql

# 方法二：使用MySQL客户端工具导入（如Navicat、DBeaver等）
```

### 2. 数据库表结构说明

导入的数据库脚本包含以下核心表：
- `class_table` - 班级表
- `teacher_table` - 教师表
- `student_table` - 学生表
- `course_table` - 课程表
- `score_table` - 成绩表

详细的表结构和字段说明请参考《学生管理系统_表结构与关联关系说明.md》文档。

## 三、后端服务部署

### 1. 后端项目构建

#### 1.1 准备工作

确保已安装JDK和Maven，并正确配置环境变量：

```bash
# 检查JDK版本
java -version

# 检查Maven版本
mvn -version
```

#### 1.2 配置数据库连接

在构建前，需要修改后端项目的数据库连接配置。找到`application.yml`或`application.properties`文件，修改以下配置：

**application.yml示例**：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_management?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: student
    password: Student@123
    driver-class-name: com.mysql.cj.jdbc.Driver
```

#### 1.3 构建项目

在后端项目根目录下执行以下命令进行构建：

```bash
# 清理并构建项目
mvn clean package -DskipTests
```

构建成功后，会在`target`目录下生成可执行的JAR包，例如：`student-management-backend-1.0.0.jar`。

### 2. 后端服务部署

#### 2.1 直接运行JAR包

**Windows环境**：

```batch
java -jar student-management-backend-1.0.0.jar
```

**Linux环境**：

```bash
java -jar student-management-backend-1.0.0.jar
```

#### 2.2 配置为系统服务（Linux）

创建服务配置文件：

```bash
vi /etc/systemd/system/student-management.service
```

文件内容如下：

```ini
[Unit]
Description=Student Management System Backend
After=syslog.target
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/student-management
ExecStart=/usr/bin/java -jar /opt/student-management/student-management-backend-1.0.0.jar
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

设置服务：

```bash
# 重新加载systemd配置
systemctl daemon-reload

# 启动服务
systemctl start student-management

# 设置开机自启
systemctl enable student-management

# 查看服务状态
systemctl status student-management
```

### 3. 验证后端服务

后端服务启动后，可以通过以下方式验证：

```bash
# 检查服务是否启动成功
curl http://localhost:8080/api/common/health

# 检查API接口是否可访问
curl http://localhost:8080/api/student/list?page=1&limit=10
```

## 四、前端服务部署

### 1. 前端项目构建

#### 1.1 准备工作

确保已安装Node.js和npm，并正确配置环境变量：

```bash
# 检查Node.js版本
node -v

# 检查npm版本
npm -v
```

#### 1.2 安装依赖

在前端项目根目录下执行以下命令安装依赖：

```bash
# 安装依赖
npm install
```

#### 1.3 配置API地址

修改前端项目中的API基础地址配置。通常在`src/api/index.js`或`src/config/index.js`文件中：

```javascript
// 修改API基础地址
const API_BASE_URL = 'http://localhost:8080/api';
```

#### 1.4 构建项目

在前端项目根目录下执行以下命令进行构建：

```bash
# 构建项目
npm run build
```

构建成功后，会在`dist`目录下生成静态文件。

### 2. 使用Nginx部署前端

#### 2.1 Nginx配置

创建或修改Nginx配置文件：

**Windows环境**：找到Nginx安装目录下的`conf/nginx.conf`文件
**Linux环境**：通常在`/etc/nginx/nginx.conf`或`/etc/nginx/conf.d/`目录下创建配置文件

配置示例：

```nginx
server {
    listen       80;
    server_name  localhost;

    # 前端静态文件目录
    root   /path/to/dist;
    index  index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 代理API请求到后端服务
    location /api {
        proxy_pass http://localhost:8080/api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

#### 2.2 启动Nginx

**Windows环境**：

```batch
# 启动Nginx
nginx.exe

# 重新加载配置
nginx.exe -s reload
```

**Linux环境**：

```bash
# 启动Nginx
systemctl start nginx

# 重新加载配置
systemctl reload nginx

# 设置开机自启
systemctl enable nginx
```

### 3. 验证前端服务

前端服务部署后，可以通过浏览器访问以下地址验证：

```
http://localhost
```

## 五、系统集成测试

### 1. 功能测试

#### 1.1 登录测试
- 访问系统登录页面：http://localhost
- 使用默认账号密码登录（如：admin/admin）
- 验证登录成功后是否能正确跳转至系统首页

#### 1.2 学生管理模块测试
- 测试学生列表页面的展示、搜索、筛选功能
- 测试学生新增、编辑、删除功能
- 测试学生详情查看功能
- 测试学生成绩管理功能

#### 1.3 班级管理模块测试
- 测试班级列表页面的展示、搜索、筛选功能
- 测试班级新增、编辑、删除功能
- 测试班级学生管理功能

#### 1.4 课程管理模块测试
- 测试课程列表页面的展示、搜索、筛选功能
- 测试课程新增、编辑、删除功能
- 测试课程成绩管理功能

#### 1.5 成绩管理模块测试
- 测试成绩列表页面的展示、搜索、筛选功能
- 测试成绩新增、编辑、删除功能
- 测试成绩批量录入功能
- 测试成绩统计分析功能
- 测试成绩导出功能

### 2. 接口测试

可以使用Postman或Swagger等工具进行API接口测试。

#### 2.1 Swagger接口文档

访问Swagger接口文档地址：

```
http://localhost:8080/swagger-ui.html
```

通过Swagger界面可以查看所有API接口的详细信息，并进行在线测试。

#### 2.2 使用Postman测试

**学生列表接口测试**：
- 方法：GET
- URL：http://localhost:8080/api/student/list?page=1&limit=10
- 验证响应数据是否正确

**新增学生接口测试**：
- 方法：POST
- URL：http://localhost:8080/api/student
- 请求体：
```json
{
  "studentCode": "2023001",
  "studentName": "张三",
  "gender": "男",
  "age": 20,
  "className": "软件工程1班"
}
```
- 验证响应状态和数据

### 3. 性能测试

#### 3.1 页面加载性能
- 使用浏览器开发工具的Performance标签分析页面加载性能
- 优化加载缓慢的资源

#### 3.2 API响应性能
- 使用JMeter或Apache Benchmark工具测试API接口的响应性能
- 优化响应时间过长的接口

## 六、常见问题及解决方案

### 1. 数据库连接问题

**问题**：后端服务无法连接到数据库

**解决方案**：
- 检查数据库服务是否已启动
- 验证数据库连接配置是否正确（地址、端口、用户名、密码）
- 确保数据库用户有足够的权限
- 检查防火墙是否允许数据库端口的连接

### 2. 服务启动失败

**问题**：后端服务启动失败，出现异常信息

**解决方案**：
- 检查JDK版本是否兼容
- 检查端口是否被占用（默认8080）
- 查看日志文件中的详细错误信息
- 确保所有依赖已正确配置

### 3. 前端页面无法正常访问

**问题**：前端页面显示空白或404错误

**解决方案**：
- 检查Nginx配置是否正确
- 验证前端静态文件路径是否正确
- 检查Nginx服务是否正常运行
- 清除浏览器缓存后重试

### 4. API请求失败

**问题**：前端发起API请求失败，控制台显示错误

**解决方案**：
- 检查API基础地址配置是否正确
- 验证后端服务是否正常运行
- 检查Nginx的API代理配置是否正确
- 查看浏览器控制台的错误信息，定位具体问题

### 5. 功能操作失败

**问题**：执行某些功能操作时失败，没有得到预期结果

**解决方案**：
- 检查操作权限是否正确配置
- 查看前端控制台和后端日志的错误信息
- 验证数据完整性和有效性
- 检查业务规则是否正确实现

## 七、系统维护和监控

### 1. 日志管理

#### 1.1 后端日志
- 默认日志目录：`logs/`
- 主要日志文件：`student-management.log`
- 可在`application.yml`中配置日志级别和输出格式

#### 1.2 前端日志
- 使用浏览器开发工具的Console标签查看前端日志
- 生产环境可配置前端错误日志收集

### 2. 定期维护任务

- **数据备份**：定期备份数据库，建议每天自动备份
- **日志清理**：定期清理日志文件，避免磁盘空间占用过大
- **系统更新**：根据需要更新系统版本和依赖库
- **安全扫描**：定期进行安全扫描，及时修复安全漏洞

### 3. 性能监控

- **数据库性能**：监控SQL执行效率，定期优化慢查询
- **应用性能**：监控JVM性能、内存使用情况
- **系统资源**：监控服务器CPU、内存、磁盘使用情况

## 八、升级与扩展

### 1. 系统升级步骤

1. **备份数据**：升级前务必备份数据库和配置文件
2. **停止服务**：停止后端服务和前端服务
3. **更新代码**：部署新版本的后端JAR包和前端静态文件
4. **更新数据库**：执行必要的数据库脚本，进行数据迁移
5. **重启服务**：启动后端服务和前端服务
6. **验证功能**：测试主要功能是否正常

### 2. 系统扩展建议

- **添加新模块**：按照现有的模块结构和代码规范，添加新的业务模块
- **集成第三方服务**：可以集成短信服务、邮件服务等增强系统功能
- **扩展报表功能**：添加更多统计报表和数据可视化功能
- **移动端适配**：开发手机App或响应式页面，支持移动端访问
- **微服务改造**：对于大型项目，可以考虑拆分为微服务架构

## 九、安全注意事项

1. **数据安全**：敏感数据加密存储，定期数据备份
2. **访问控制**：严格的权限管理，最小权限原则
3. **防SQL注入**：使用参数化查询，避免SQL注入攻击
4. **防XSS攻击**：对用户输入进行过滤和转义
5. **HTTPS配置**：生产环境建议配置HTTPS协议
6. **定期安全审计**：检查系统日志，发现异常访问行为

---

通过以上部署和测试指南，您应该能够顺利部署学生管理系统并进行功能验证。如果在部署或使用过程中遇到任何问题，请参考常见问题部分的解决方案，或联系技术支持。