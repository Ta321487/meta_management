# 学生管理系统操作指南

本指南将帮助您使用元数据管理系统配置一个完整的学生管理系统。

## 系统概述

学生管理系统需要包含以下核心功能：
- 学生信息管理（增删改查）
- 课程信息管理（可选）
- 成绩管理（可选，需要关联学生和课程）

## 操作步骤

### 第一步：创建表结构

#### 1.1 创建学生表（STUDENT_TABLE）

进入 **表管理** 页面，点击"新增表"，填写以下信息：

- **表编码**: `STUDENT_TABLE`
- **表名称**: `学生信息表`
- **主键策略**: `AUTO`（自增）
- **描述**: `存储学生基本信息`

#### 1.2 创建课程表（可选，COURSE_TABLE）

如果需要管理课程信息，创建课程表：

- **表编码**: `COURSE_TABLE`
- **表名称**: `课程信息表`
- **主键策略**: `AUTO`
- **描述**: `存储课程基本信息`

#### 1.3 创建成绩表（可选，SCORE_TABLE）

如果需要管理学生成绩，创建成绩表：

- **表编码**: `SCORE_TABLE`
- **表名称**: `成绩信息表`
- **主键策略**: `AUTO`
- **描述**: `存储学生成绩信息，关联学生和课程`

---

### 第二步：配置表字段

#### 2.1 配置学生表字段

进入 **字段管理** 页面，选择表 `STUDENT_TABLE`，然后添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 |
|---------|---------|---------|--------|---------|---------|------|
| `STU_ID` | `id` | `bigint` | `主键ID` | 否 | `input` | 0 |
| `STU_CODE` | `student_code` | `varchar(50)` | `学号` | 是 | `input` | 1 |
| `STU_NAME` | `student_name` | `varchar(100)` | `姓名` | 是 | `input` | 2 |
| `STU_GENDER` | `gender` | `varchar(10)` | `性别` | 是 | `select` | 3 |
| `STU_AGE` | `age` | `int` | `年龄` | 否 | `number` | 4 |
| `STU_PHONE` | `phone` | `varchar(20)` | `手机号` | 否 | `input` | 5 |
| `STU_EMAIL` | `email` | `varchar(100)` | `邮箱` | 否 | `input` | 6 |
| `STU_ADDRESS` | `address` | `varchar(200)` | `地址` | 否 | `textarea` | 7 |
| `STU_CLASS` | `class_name` | `varchar(50)` | `班级` | 否 | `input` | 8 |
| `STU_ENTRY_DATE` | `entry_date` | `date` | `入学日期` | 否 | `datepicker` | 9 |

**字段配置说明**：
- 学号（STU_CODE）：必填，用于唯一标识学生
- 姓名（STU_NAME）：必填
- 性别（STU_GENDER）：必填，使用下拉框组件，可在校验规则中配置选项
- 年龄（STU_AGE）：数字类型，使用数字输入框
- 手机号（STU_PHONE）：可在校验规则中添加手机号格式验证
- 邮箱（STU_EMAIL）：可在校验规则中添加邮箱格式验证
- 地址（STU_ADDRESS）：使用文本域组件
- 入学日期（STU_ENTRY_DATE）：使用日期选择器

**校验规则示例**（JSON格式）：
- 性别字段：`{"options": ["男", "女"]}`
- 手机号：`{"pattern": "^1[3-9]\\d{9}$", "message": "请输入正确的手机号"}`
- 邮箱：`{"pattern": "^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$", "message": "请输入正确的邮箱地址"}`

#### 2.2 配置课程表字段（可选）

如果创建了课程表，选择表 `COURSE_TABLE`，添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 |
|---------|---------|---------|--------|---------|---------|------|
| `COURSE_ID` | `id` | `bigint` | `主键ID` | 否 | `input` | 0 |
| `COURSE_CODE` | `course_code` | `varchar(50)` | `课程编号` | 是 | `input` | 1 |
| `COURSE_NAME` | `course_name` | `varchar(100)` | `课程名称` | 是 | `input` | 2 |
| `COURSE_TEACHER` | `teacher` | `varchar(50)` | `授课教师` | 否 | `input` | 3 |
| `COURSE_CREDIT` | `credit` | `decimal(3,1)` | `学分` | 否 | `number` | 4 |
| `COURSE_HOURS` | `hours` | `int` | `课时` | 否 | `number` | 5 |

#### 2.3 配置成绩表字段（可选）

如果创建了成绩表，选择表 `SCORE_TABLE`，添加以下字段：

| 字段编码 | 字段名称 | 字段类型 | 显示名 | 是否必填 | 表单组件 | 排序 |
|---------|---------|---------|--------|---------|---------|------|
| `SCORE_ID` | `id` | `bigint` | `主键ID` | 否 | `input` | 0 |
| `SCORE_STU_ID` | `student_id` | `bigint` | `学生ID` | 是 | `select` | 1 |
| `SCORE_COURSE_ID` | `course_id` | `bigint` | `课程ID` | 是 | `select` | 2 |
| `SCORE_VALUE` | `score` | `decimal(5,2)` | `成绩` | 是 | `number` | 3 |
| `SCORE_TERM` | `term` | `varchar(20)` | `学期` | 否 | `input` | 4 |
| `SCORE_DATE` | `exam_date` | `date` | `考试日期` | 否 | `datepicker` | 5 |

---

### 第三步：创建模块

进入 **模块管理** 页面，点击"新增模块"，创建学生管理模块：

- **模块编码**: `STUDENT_MODULE`
- **模块名称**: `学生管理模块`
- **模块类型**: `DATA_MANAGE`（数据管理型）
- **描述**: `学生信息管理模块，支持学生信息的增删改查操作`
- **关联表**: 选择 `STUDENT_TABLE`（如果创建了课程表和成绩表，也可以一并关联）

**说明**：
- 模块类型选择"数据管理型"，系统会自动创建列表页、表单页、详情页等功能节点
- 如果后续需要添加课程管理和成绩管理，可以创建单独的模块，或者在同一模块中关联多个表

---

### 第四步：配置表关联关系（可选）

如果创建了多个表，需要配置它们之间的关联关系。

#### 4.1 配置学生与成绩的关联

进入 **表关联关系管理** 页面，点击"新增关联"：

- **关联编码**: `REL_STUDENT_SCORE`
- **关联名称**: `学生-成绩关联`
- **主表**: `STUDENT_TABLE`
- **主表关联字段**: `STU_ID`（学生表的ID字段）
- **从表**: `SCORE_TABLE`
- **从表外键字段**: `SCORE_STU_ID`（成绩表的学生ID字段）
- **关联类型**: `ONE_TO_MANY`（一对多，一个学生可以有多个成绩记录）

#### 4.2 配置课程与成绩的关联

- **关联编码**: `REL_COURSE_SCORE`
- **关联名称**: `课程-成绩关联`
- **主表**: `COURSE_TABLE`
- **主表关联字段**: `COURSE_ID`
- **从表**: `SCORE_TABLE`
- **从表外键字段**: `SCORE_COURSE_ID`
- **关联类型**: `ONE_TO_MANY`（一对多，一个课程可以有多个成绩记录）

---

### 第五步：配置功能节点（可选）

进入 **功能节点管理** 页面，可以为模块配置额外的功能节点：

1. **列表页节点**（通常已自动创建）
   - 节点编码: `STUDENT_LIST`
   - 节点名称: `学生列表页`
   - 节点类型: `LIST_PAGE`
   - 关联表: `STUDENT_TABLE`

2. **表单页节点**（通常已自动创建）
   - 节点编码: `STUDENT_FORM`
   - 节点名称: `学生表单页`
   - 节点类型: `FORM_PAGE`
   - 关联表: `STUDENT_TABLE`

3. **详情页节点**（通常已自动创建）
   - 节点编码: `STUDENT_DETAIL`
   - 节点名称: `学生详情页`
   - 节点类型: `DETAIL_PAGE`
   - 关联表: `STUDENT_TABLE`

---

### 第六步：配置业务规则（可选）

进入 **业务规则管理** 页面，可以配置一些业务规则，例如：

1. **学号唯一性规则**
   - 规则编码: `RULE_STU_CODE_UNIQUE`
   - 规则类型: `VALIDATION_RULE`
   - 规则内容（JSON）:
   ```json
   {
     "field": "student_code",
     "type": "unique",
     "message": "学号已存在"
   }
   ```

2. **成绩范围规则**
   - 规则编码: `RULE_SCORE_RANGE`
   - 规则类型: `VALIDATION_RULE`
   - 规则内容（JSON）:
   ```json
   {
     "field": "score",
     "type": "range",
     "min": 0,
     "max": 100,
     "message": "成绩必须在0-100之间"
   }
   ```

---

### 第七步：生成业务系统代码

配置完成后，可以使用**代码生成器**生成实际可用的业务系统代码。

#### 7.1 进入代码生成页面

1. 在左侧菜单中点击"代码生成"
2. 选择要生成代码的表（如 `STUDENT_TABLE`）
3. 输入Java包名（如 `com.example`）

#### 7.2 生成代码

**方式一：生成所有代码（推荐）**
- 点击"生成所有代码"按钮
- 系统会自动生成以下文件：
  - `create_table.sql` - 数据库建表SQL
  - `Entity.java` - Java实体类
  - `Controller.java` - 控制器类
  - `Service.java` - 服务类
  - `Mapper.java` - Mapper接口
  - `Mapper.xml` - MyBatis映射文件
  - `List.vue` - Vue列表页面
  - `Form.vue` - Vue表单页面

**方式二：单独生成**
- 在对应的标签页中点击"生成"按钮
- 可以单独生成某个文件

#### 7.3 复制和使用代码

1. 生成代码后，点击"复制"按钮将代码复制到剪贴板
2. 将代码粘贴到对应的项目文件中：
   - SQL文件：在数据库中执行建表SQL
   - Java文件：放入对应的包路径下
   - Vue文件：放入前端项目的views目录
3. 根据实际需求调整代码（如添加业务逻辑、样式等）

#### 7.4 代码生成API（可选）

也可以通过API直接获取代码：

- `GET /api/codegen/sql/{tableCode}` - 生成SQL
- `GET /api/codegen/entity/{tableCode}?packageName=com.example` - 生成实体类
- `GET /api/codegen/controller/{tableCode}?packageName=com.example` - 生成Controller
- `GET /api/codegen/service/{tableCode}?packageName=com.example` - 生成Service
- `GET /api/codegen/mapper/{tableCode}?packageName=com.example` - 生成Mapper接口
- `GET /api/codegen/mapperxml/{tableCode}?packageName=com.example` - 生成Mapper XML
- `GET /api/codegen/vue/list/{tableCode}` - 生成Vue列表页
- `GET /api/codegen/vue/form/{tableCode}` - 生成Vue表单页
- `GET /api/codegen/all/{tableCode}?packageName=com.example` - 生成所有代码

---

### 第八步：导出元数据（可选）

如果需要导出元数据配置（JSON格式），可以通过以下方式：

1. **导出单个模块元数据**
   - 访问API: `GET /api/metadata/module/STUDENT_MODULE`
   - 获取学生管理模块的完整元数据配置

2. **导出所有元数据**
   - 访问API: `GET /api/metadata/export/all`
   - 获取系统中所有模块、表、字段、关联关系的完整配置

3. **导出表字段配置**
   - 访问API: `GET /api/metadata/table/STUDENT_TABLE/fields`
   - 获取学生表的所有字段配置

---

## 配置示例总结

### 最小配置（仅学生管理）

1. ✅ 创建表：`STUDENT_TABLE`
2. ✅ 配置字段：学号、姓名、性别、年龄等
3. ✅ 创建模块：`STUDENT_MODULE`，关联 `STUDENT_TABLE`

### 完整配置（学生+课程+成绩）

1. ✅ 创建表：`STUDENT_TABLE`、`COURSE_TABLE`、`SCORE_TABLE`
2. ✅ 配置各表字段
3. ✅ 创建模块：`STUDENT_MODULE`
4. ✅ 配置关联关系：学生-成绩、课程-成绩

---

## 注意事项

1. **编码规范**：
   - 表编码、字段编码、模块编码使用大写字母和下划线
   - 编码必须唯一，不能重复

2. **字段类型**：
   - 使用标准的数据库类型，如 `varchar(100)`、`int`、`bigint`、`date`、`decimal(5,2)` 等

3. **主键字段**：
   - 主键字段通常不需要手动创建，系统会根据主键策略自动生成
   - 如果使用自增策略，主键字段类型应为 `bigint`

4. **关联关系**：
   - 配置关联关系时，确保主表和从表的关联字段类型一致
   - 一对多关系：一个主表记录对应多个从表记录
   - 一对一关系：一个主表记录对应一个从表记录

5. **校验规则**：
   - 校验规则使用JSON格式
   - 可以配置正则表达式、范围、必填等验证规则

---

## 后续操作

配置完成后，您可以：

1. **生成业务代码**：使用代码生成器生成完整的业务系统代码（SQL、Java、Vue）
2. **查看操作日志**：在"操作日志"页面查看所有配置变更记录
3. **导出元数据**：将配置的元数据导出为JSON格式（可选）
4. **修改配置**：随时可以编辑、删除已创建的配置项，重新生成代码

## 交付使用

完成配置和代码生成后，您可以：

1. **交付给开发团队**：
   - 提供生成的SQL文件，用于创建数据库表
   - 提供生成的Java代码，集成到后端项目
   - 提供生成的Vue代码，集成到前端项目

2. **交付给客户**：
   - 提供完整的业务系统代码包
   - 包含数据库建表脚本
   - 包含前后端完整代码
   - 客户可以直接部署使用

3. **持续维护**：
   - 如需修改业务逻辑，可在元数据管理系统中调整配置
   - 重新生成代码，替换原有文件
   - 支持版本管理和配置变更追踪

---

## 快速开始检查清单

- [ ] 创建学生表（STUDENT_TABLE）
- [ ] 为学生表添加字段（至少包含学号、姓名、性别）
- [ ] 创建学生管理模块（STUDENT_MODULE）
- [ ] 将学生表关联到模块
- [ ] （可选）创建课程表和成绩表
- [ ] （可选）配置表关联关系
- [ ] 导出元数据验证配置

完成以上步骤后，您就成功配置了一个学生管理系统的元数据模型！

