<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metadata.mapper.MetadataAdminMapper">

    <resultMap id="BaseResultMap" type="com.metadata.entity.MetadataAdmin">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="last_login_time" property="lastLoginTime"/>
    </resultMap>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM metadata_admin WHERE username = #{username}
    </select>

    <update id="updatePassword">
        UPDATE metadata_admin SET password = #{password} WHERE username = #{username}
    </update>

    <update id="updateLastLoginTime">
        UPDATE metadata_admin SET last_login_time = NOW() WHERE username = #{username}
    </update>

</mapper>

